# 待确认问题清单（给编剧 / 策划）

> 提给：**编剧**（1–6 项）与**策划**（7–8 项）
> 来自：框架同学
> 背景：D1–D7 全流程、四个结局、存档与真浏览器验收都已完成。下面每一条都是
> "脚本里没写清 / 前后矛盾 / 暂时由开发替你选了一个"，**改口之后只需要动内容层，不会动引擎**。
> 请在每条的「你的决定」处写一句就行。

---

## 1 ⭐ 最重要：脚本的《目标心情》表和它自己的干预数值对不上

**问题**：脚本第三章给了《目标心情》表（第 341 行）：

```text
D1   D2   D3   D4   D5   D6   D7
45   40   55   35   50   28   70
```

这条曲线隐含"一周净变化 +25"。但脚本各处给的**单次干预数值**（+12 / +10 / +6 …）
和**每日守护之光**（第 221、245 行：D1–D5 每天 2 点，D6 给 3 点）放在一起算，
一周最多能成功干预 12 次、只能强制 miss 5 次，净变化是 **+80**。

**这不是实现问题，是脚本内部两处数字互相矛盾**：不管怎么等比缩放，都变不成 +25
（缩放只能让整条曲线一起靠近起点 45）。

**实测数据**（每步都选最优）：周终心情落在 **91**，而《目标心情》表要求 70。

**另外**：0–100 的量表在原始数值下会被打满到 98，导致"选哪个干预"在数值上几乎看不出区别。

**现在临时怎么做的**：把所有心情数值统一 **×0.5**（`content/moments.ts` 的 `MOOD_SCALE`），
保持脚本设计的相对强弱和正负号不变，把峰值从 98 压到 91。

**请你选一个**：

- [ ] **A. 把成功干预的数值降下来**（例如 +12 → +5、+10 → +4），miss 保持原样。
      最贴近《目标心情》表，但改动量最大，需要你重写一遍数值。
- [ ] **B. 把守护之光减到每天 1 点**（D6 给 2 点）。不用改任何台词数值，
      但玩家的干预机会从 12 次降到 7 次左右，"能做的事变少"会明显影响手感。
- [ ] **C. 承认《目标心情》表是"玩家没有干预时的基线"**，
      把它改成"有干预的曲线"（峰值抬到 90 附近）。改表不改数值，最省事。
- [ ] **D. 维持现状**（已做 ×0.5 缩放），我知道峰值是 91 不是 70。

**你的决定**：＿＿＿＿＿＿＿＿＿＿＿＿

---

## 2 ⭐ D7 终章点题句，脚本自己前后不一致

**问题**：同一份脚本里有两句互相冲突的定稿——

| 位置 | 内容 |
|---|---|
| 第 303 行（D7 正文） | 「**谢谢你守护我。**」 |
| 第 368 行（文末改稿记录） | 「「谢谢你守护我。」**改为**「**感谢你，撑过来了。**」（终稿点题句）」 |
| 第 369 行（文末改稿记录） | 文风样板末句删「守护我」，改为「**谢谢你。**」 |

**现在临时怎么做的**：按**改稿记录**取「感谢你，撑过来了。」，并在自动化测试里
断言终章**不得**再出现「谢谢你守护我。」。

**请你确认**：

- [ ] 以改稿记录为准 →「感谢你，撑过来了。」（保持现状，不用改代码）
- [ ] 以 D7 正文为准 →「谢谢你守护我。」（我会同时改掉测试里那条反向断言）

**你的决定**：＿＿＿＿＿＿＿＿＿＿＿＿

---

## 3 「平均心情」没有定义口径，而且阈值 66 不可达

**问题一（口径）**：结局表（第 322–324 行）只写"平均心情 ≥66 / ≥42"，没说平均什么：

- ① **所有心情记录点的平均**（每个时刻结算后记一次，一周约 50 个点）
- ② **每天锚点的平均**（只记 7 个点，事件多的日子权重变小）

两种口径差别很大。

**问题二（阈值）**：不管用哪种口径，**66 都够不到**。脚本自己的曲线是
45 起步 → D6 掉到 28 → 末尾回到 91，即使每步都选最优，① 算出来也只有 **64.9**。
也就是说脚本的阈值高于它自己的曲线能给出的上限。

**现在临时怎么做的**：用口径 ①（记录点平均），并把晴空阈值从 **66 降到 62**。
改后"七天全程尽力 + 羁绊满 12"才拿得到晴空，摸鱼会落到生长 / 雨过（实测阶梯见下）。

**请你选一个**：

- [ ] 口径 ①（记录点平均），阈值 62（保持现状）
- [ ] 口径 ②（每日锚点平均），阈值维持 66（我会重跑一遍配平再报数）
- [ ] 其他阈值：＿＿＿

**你的决定**：＿＿＿＿＿＿＿＿＿＿＿＿

---

## 4 隐藏结局「笨拙的守护」的触发次数，上限只有 6

**问题**：脚本（第 325 行）写「单一干预累计 ≥5 次」。但实测一周里，
**任何单一干预最多只能被选中 6 次**（`偷加两小时` = 6、`捂耳朵` = 5，其余 ≤4），
因为每天的光会重置、不累积。

阈值定 5 会跟"最优解"撞车 —— `捂耳朵` 在它出现的 5 个事件里都是收益最高的选项，
认真玩的玩家自然会点满 5 次，于是**隐藏结局变成默认结局，反而盖住了晴空**。

**现在临时怎么做的**：阈值改成 **6**。这样只有"每天都把光留给夜里那次偷加两小时"的玩家
才拿得到，代价是白天大量放手（实测平均心情只有 28.8、错过 11 次）——
正好对上「你总是用同一种方式，笨笨地护着我。」这层意思。
**不能定 7 以上**，那会超过内容上限 6、变成永远拿不到的死结局。

**请你确认**：

- [ ] 阈值 6（保持现状）
- [ ] 阈值 5，但**接受隐藏结局会成为最常见的结局**
- [ ] 换个规则，例如"同一种干预 ≥5 次 **且** 占全部干预的一半以上"（我会重算可达性）

**你的决定**：＿＿＿＿＿＿＿＿＿＿＿＿

---

## 5 终章「低羁绊简化版」没有触发条件

**问题**：脚本第 305 行写「（低羁绊周目此处简化为：「……好像，一直有人在我身边。」「谢谢。」）」，
但**没给羁绊阈值**。

**现在临时怎么做的**：**没有实现这个分支**，所有周目都走完整版终章。

**请你给出**：

- [ ] 低羁绊的阈值：羁绊 ≤ ＿＿ 时走简化版
- [ ] 不做这个分支（所有周目统一走完整版终章）

**你的决定**：＿＿＿＿＿＿＿＿＿＿＿＿

---

## 6 D2「体育课改自习」只有一句，没有后续

**问题**：脚本第 130–134 行要求这个时刻**不可干预**、心情 −10，并只给了一句
「好不容易的放松时间，又没了。」。但没写这句之后是否要补一句安慰 / 转场。

**现在临时怎么做的**：只播这一句就结算，直接进入下一个时刻。

**请你给出**：

- [ ] 就这一句，不用补（保持现状）
- [ ] 补一句，内容是：＿＿＿＿＿＿＿＿＿＿＿＿

**你的决定**：＿＿＿＿＿＿＿＿＿＿＿＿

---

## 6.5 作息定制原本对玩法毫无影响（**已修，请你确认判定值**）

**问题**：脚本第 48 行《睡眠规则》写：

> 睡眠时长 = 闹钟 − 熄灯（跨夜计算）；**< 7.25 小时即每晚触发「睡眠不足」事件，越短 miss 惩罚越重（数值侧验算）**。

但之前 6 个「睡眠不足」事件是**无条件触发**的 ——
开局定制里的"闹钟"和"熄灯"两道题**填什么都一样**：
玩家把睡眠拉到 10 小时，夜里照样报"睡眠不足"。

**已经怎么修的**：按脚本给这 6 个事件补上了 < 7.25 小时的门控（`content/rules.ts` 的 `sleepShort`）。
实测：睡够 7.25 小时 → 一周少发生 6 个「睡眠不足」（43 → 37 个时刻）；睡不够 → 6 个全都触发。

**但"越短惩罚越重"的具体数值脚本没写**，我现在用的是**各时刻固定惩罚**
（−6 / −7 / −8 / −10 / −8 / −10，就是脚本各时刻原本标注的值）。

**请你选一个**：

- [ ] 就按脚本各时刻标注的固定惩罚（保持现状）
- [ ] 真要"越短惩罚越重"，请给分档，例如 睡眠 ≥7h → miss −4；6–7h → −7；<6h → −10（按你的设计改）
- [ ] 其他：＿＿＿＿＿＿＿＿＿＿＿＿

**你的决定**：＿＿＿＿＿＿＿＿＿＿＿＿

---

## 6.6 D5 羁绊检查点原本只做了一半（**已修，请确认阈值**）

**问题**：脚本第 236–239 行 D5 21:00 写了**两个版本**：

| 条件 | 台词 | 心情 | 云朵 |
|---|---|---|---|
| 羁绊 ≥ 6 | 「像是有谁，一直在。」 | +5 | 金 |
| 羁绊 < 6 | 「……今天，也就这样吧。」 | +0 | 白 |

但之前实现里**只有"羁绊 < 6"那一版，而且是无条件触发** ——
等于整条羁绊线在这一刻完全失效，金色那句 ★ 也永远拿不到。

**已经怎么修的**：按脚本补成两个互斥分支（羁绊 ≥6 走金色版 +5，否则白色版）。
实测：尽力玩（羁绊 12）拿到金色句；只救前 2 天（羁绊 4）拿到白色句。

**请你确认**：

- [ ] 就按脚本的羁绊 ≥6 分界（保持现状）
- [ ] 换一个阈值：羁绊 ≥ ＿＿

**你的决定**：＿＿＿＿＿＿＿＿＿＿＿＿

---

## 6.7 开局问卷只剩一题：只问「那时候的TA，是——」（**已改，请你知悉**）

**问题**：脚本第一节写的是「开局定制（7 问）」：性别、发型、校服配色、闹钟、熄灯、补课日、放假频率。
验收反馈要求**只保留性别一题**（其余 5 问删掉），理由是这 5 问在观感上没有对应的画面差异 ——
玩家认真选完发型与校服配色，进游戏后看到的还是同一张小人的图。

**已经怎么改的**：

| 原来 | 现在 |
|---|---|
| 7 问（4 选项 + 1 配色 + 2 滑杆） | 1 问：`那时候的TA，是——`（男生 / 女生，两张正式立绘） |
| `state.custom` 里有 `gender/hair/uniform/…` | 只剩 `gender` 一项 |
| 作息由玩家填 | **固定默认值**：23:30 熄灯 → 05:50 起床（睡眠 380 分钟 = 6 小时 20 分） |

**三件请你知悉、可能需要你回一句的事**：

1. **终章的记忆回响变量只剩 `{性别}`**。
   脚本里那套模板变量 `{性别}{发型}{校服配色}{闹钟}{熄灯}{补课日}{放假频率}` 现在只能填出性别。
   现在的终章用的是「那时候的TA，是女生。」+「谢谢你，撑过来了。」两句。
   如果你希望回响更有内容，请给两句**不依赖那 5 个变量**的文案。

2. **作息固定成"睡不够"那条线**。默认 6 小时 20 分 < 7.25 小时，
   所以那一周里 6 个「睡眠不足」时刻**每次都会触发**（见 6.5 的门控）——
   玩家不再有"睡够一点"的选项。若你希望默认值是"睡够"，
   请指定默认闹钟/熄灯时间（改 `content/customization.ts` 一处即可）。

3. **男女主角的图要各交一套**。选了男生只出现男生的图、选了女生只出现女生的图
   （美术交付说明 §一.2 的硬要求，已有自动化测试守着）。
   目前 `art.d1.girl.down`（女生低头）还没交，女生的"被批评后低头"这一刻会**回退成她的紧绷图**。

**请你确认**：

- [ ] 就按"只问性别 + 作息固定 23:30–05:50"（保持现状）
- [ ] 默认作息改成睡够：闹钟 ＿＿ / 熄灯 ＿＿
- [ ] 终章回响换文案：＿＿＿＿＿＿＿＿＿＿＿＿

**你的决定**：＿＿＿＿＿＿＿＿＿＿＿＿

---

## 6.8 D2–D3 剧情图接入时发现的几处对不上（**请你/美工确认**）

**背景**：美工按天交来的 `如果有你_DAY2/3人物素材` 已经接进游戏（13 张新图，
登记表见 `content/storyArt.ts`）。接的过程中发现四处"图与剧情对不上"，都按下面临时的办法处理了。

**① D2 12:40 那一刻：图在食堂，剧情在教室。**
说明书写「`day2/*/…食堂吃饭.jpg` → D2 12:40 食堂午饭」，
但代码里 D2_1240 的地点与文案是**教室**（标题「午休」/ 台词「今天的菜里，有我爱吃的。」）。
现在那一刻的画面是食堂、左上角地点名却写着"教室"。

- [ ] 改剧情：把 D2_1240 的地点/文案改成食堂（我同步改 `content/moments.ts`）
- [ ] 改画面：换成"在教室里吃饭"的图
- [ ] 就这样（这一刻本来就该有食堂的画面）

**② ✅ 已按"缺图就用别的图顶上"处理：D4 17:00「黄昏」用 D1 的「平静」插画代替。**
DAY4 说明书里写了要用 `男生_抬头_复用.png` / `女生_抬头_复用.png`（"主角抬头看粉笔灰或窗外"），
但**这两个文件不在交付包里**。以前的做法是"这一刻不插画"，画面只剩黄昏教室的空镜
（底部还立着主角立绘，所以不是完全没人）。
现在的做法是**用 D1 的 `art.d1.<who>.calm`（平静）顶上** —— 它本来就是一张透明抠图，
所以是"人物直接站在黄昏教室里"，而不是一张插画卡。选「平静」而不是 14:00 那张「放空」的理由：
14:00 刚用过「放空」，同一张脸三小时里出现两次更假。说明书里这张本来就叫「抬头_复用」——
**复用一张 D1 的动作图，正是它的原意**。

- [ ] 就这样（用 D1「平静」顶到补图为止）
- [ ] 补交「抬头」两张（交来我改成它们，代码只差一行）
- [ ] 换成别的姿态：＿＿＿＿＿＿＿＿＿＿

> ⚠️ **顺带发现一处台词对不上（不是缺图，是缺句）**：脚本 D4 17:00 写了**两句** ——
> 先「黄昏的教室，是橘色的。」（无 ★、无数值），再 ★「粉笔灰在光里飘。好像，也没那么糟。」**心情 +3**。
> 游戏里只实现了第一句，并且把 +3 挂在了它身上。
> 收藏句的 ★ 应该落在**第二句**上（第一句没有 ★）。
> **这不是我这一轮能替你决定的**：要补第二句的话，请确认是"两句都要、+3 挂第二句"，
> 还是"就保留现在这一句"。（同类情况我会再做一次全脚本对照，见附六。）

**③ D4 17:00 的"抬头"图另见上一条。**

**③ 同一刻的两张图画法不一致。**
D2 08:00「成绩单来了」里，`女生_查看成绩单.png` 与 `老师_递出成绩单.png` 是**透明抠图**
（四角 alpha=0、57% / 72% 全透明像素），而 `男生_查看成绩单.jpg` 是**带背景的整幅插画**。
程序的渲染方式是两套（整幅图按"剧情卡"演出、抠图直接站在场景上），
所以**男生路线与女生路线这一刻的观感不一样**。

- [ ] 统一成透明抠图（男生那张重出）
- [ ] 统一成带背景的整幅图（女生 / 老师那张重出）
- [ ] 接受不一致（两套渲染都保留）

**④ D2 22:50「焦虑」用的是教室里的紧绷图。**
说明书写「紧绷状态：D2 10:20 拖堂说教、D2 22:50 焦虑表现」，我照办了。
但那一刻的场景是**卧室**（熄灯后的焦虑），画面里却是教室的紧绷坐姿。

- [ ] 就这样（紧绷的表情比场景更重要）
- [ ] 补一张"卧室里睡不着 / 焦虑"的图

**你的决定**：＿＿＿＿＿＿＿＿＿＿＿＿

---

## 6.9 D4–D7 剧情图接入时发现的问题（**请你/美工确认**，最重要的一条是 ①）

**背景**：DAY4–DAY7 四个素材包已经全部接进游戏（新导入 35 张图，D4–D7 的 **22 个时刻全部挂上了画面**，
`npm run art:check` 现在报 80/85 张已交付）。接的过程中有五件事需要你或美工拍板。

**① ⭐ D6 20:30 的「排名公布投影道具」不能直接用：画面里男女同框。**
交付的 `day6/common/排名公布_投影道具.jpg` 是一张 16:9 的整幅图，画面里**男生在左、女生在右
同时出现在同一帧**（还有两处红色警示角标）。而四份说明书的 §一 都写着硬要求：
"任何剧情画面不得同时出现男生和女生"。这一张无论怎么接都会破例，所以**我没有接入**，
只把 AssetId 预留成 `art.story.common.rankProjection`（1280×720）。
D6 20:30 那一刻现在用的是按性别的 `排名公布` 人物图（这张是对的）。

- [ ] 重出一张"空教室 + 幕布上投影着名次"（没有人，最稳）
- [ ] 按性别各裁一版（左半 / 右半），当成两张 `art.story.<boy|girl>.rankProjection`
- [ ] 就这样，不接投影层（只用人物图）

**② D4 17:00「黄昏」缺一张图（D2–D3 那一轮就提过，这次仍然缺）。**
DAY4 说明书 §四点名要用 `男生_抬头_复用.png` / `女生_抬头_复用.png`，
但包里**没有这两个文件**。现在那一刻只有教室空镜（黄昏粉笔灰那张，构图是橘色夕阳教室）
+ 主角小人的"抬头"体态，没有人物插画。AssetId 已预留在 `art.story.<boy|girl>.lookUp`。

- [ ] 补交这两张（交来即可接上，代码里只差一行）
- [ ] 用现有的「眼神放空」顶（意思是"发呆"，不是"抬头看"）
- [ ] 就这样，那一刻只给空镜

**③ D6 12:00「饭有点凉」的图与台词相反。**
交付的 `day6/common/教室_饭有点凉.jpg` 里，那盒饭**正在冒热气**（还画了鸡腿和卤蛋），
而这一刻的台词是「饭有点凉」。另外这张是 **1216×912（4:3）**，而同一天别的教室空镜
都是 1368×768（16:9）—— 做成整屏底图会被裁掉 25% 的画面高度，所以我把它接成了**插画卡**。

- [ ] 重出一张"饭已经凉了"（没有热气）的 16:9 空镜
- [ ] 就这样（图是好看的，热气当作"刚端上来"）
- [ ] 台词改成「饭，还是热的。」

**④ D4 23:50 的睡眠图，说明书自己给了两种说法。**
DAY4 说明书里，`上下铺皱眉睡觉` 标注"D4 23:50 深夜睡眠不足，直接复用 DAY2 图片"，
而同一份说明书末尾又说 23:50"直接使用前几天的疲惫状态"。两句话指的不是同一张图。
我取了**上下铺皱眉睡觉**，理由是 D2 23:30 / D3 23:40 用的就是它 ——
同一个"半夜醒着"的画面不该一周里换三张脸。

- [ ] 就这样（统一用上下铺）
- [ ] 统一改用"疲惫状态"（那我同步把 D2/D3/D4/D5 都换掉）

**⑤ 水印。** D4–D7 交来的 JPG 右下角普遍带生成平台的**「AI生成」角标**
（结局页那张在底部正中间）。说明书写的是"正式接入前由美术清理或裁切"，
我**没有做程序遮盖**（遮盖就要盖掉画面内容，得先知道哪些图要重出）。

- [ ] 美术统一清理 / 重出
- [ ] 用程序在右下角压一层很淡的渐变遮住（会损失一点画面）
- [ ] 接受（黑客松演示看不出来）

**⑥ 主角立绘（D1 那批）有两个问题，接 D4 之后才看得见。**
D4 17:00 的画面是**暗色的黄昏教室**，主角立绘往上一放，问题就露出来了（亮背景上看不出来）：

- **立绘四周有一圈半透明白雾**，不是干净的透明抠图。实测四张主角立绘里
  alpha 在 25–200 之间的像素占 **23–27%**（`portrait.player.boy` 22.6% 全透明 /
  26.5% 半透明 / 49.4% 实体）—— 也就是说人物抠出来了，但**背景没抠干净，留下了一层半透明的白灰**。
  在浅色场景（清晨教室）里它和背景差不多白，看不出来；在深色场景里就是一块灰云。
- **`portrait.player.boy.png` 与 `portrait.player.boy.rest.png` 是同一个文件**
  （SHA256 完全相同）。也就是说男生路线"干预成功 → 换放松态"其实**换了个寂寞**，
  画面一动不动；女生那两张是真的不一样。这说明"放松态"当时是复制过去的。

- [ ] 美工 B 重出：立绘四周抠干净（alpha 只有 0 或 255，边缘留一点抗锯齿），男生放松态补齐
- [x] ~~程序按阈值清理（alpha < 200 的直接置 0）~~ **← 本轮已经按这个方案处理了**，见下
- [ ] 就这样（黑客松演示不深究）

**已做的处理（`scripts/clean-portrait-alpha.py`，可回退）**：

四张主角立绘就地做了 alpha **二值化**（≥200 的置 255、其余置 0，再 0.8px 羽化）。
实测半透明从 **26.5% 降到 4%**、全透明从 21.5% 升到 46%，那层灰雾没有了；
真实浏览器里也加了护栏（`npm run test:browser` 会打印 alpha 分布，
半透明超过一成就算失败）。原件备份在 `art-originals/`（不是构建输入）。

两个必须记下来的坑：

- **不能做线性映射。** 第一版写的是 `(alpha − 200) / 55 × 255`（把 200→255 线性拉满），
  结果**人物整个变成半透明** —— 因为原图不只是背景脏，而是整张图被整体压过一层透明度：
  200–244 那一档占了 **24%**，人物的身体本身就落在那一档里（实测不透明像素里
  97% 其实已经 ≥ 250，但剩下那 24% 是身体的一部分）。线性映射会把它压到 139，
  在暗场景里人就变成一个幽灵。二值化则把"人物"一次性拉成 255、把"背景"一次性清零，两边都对。
- **"看着半透明"有时候是错觉。** 处理完之后在深色底上看白校服仍然发暗，一度以为没修好；
  实际把同一个不透明像素在深底和浅底上分别合成，RGB **完全相同**（那是同时对比的错觉）。
  判断这件事要看数值，不要看感觉。

**男生放松态仍是同一张图**（`portrait.player.boy.png` 与 `.rest.png` 的 SHA256 相同），
这个只能靠重出图解决 —— 也就是说男生路线"干预成功 → 换放松态"画面依旧一动不动。

**你的决定**：＿＿＿＿＿＿＿＿＿＿＿＿

---

## 6.10 本次玩法/操作改动（**请你知悉**，其中 ① 需要一个文案决定）

**背景**：这一轮按验收反馈做了四件事：干预窗口多了一个"不伸手"的选项、空格/回车全程可翻页、
删掉了开发期的占位小人、修好了 D2「成绩单来了」之后没有人图的 bug。三件是操作层的，
一件改了"每一刻画面里会有谁"。逐条如下。

**① 新增选项「这次，就让它过去」（干预窗口里的第三个按钮）。**
验收反馈要的是"加一个'不帮助'的选项，点一下就继续，省掉每次 10 秒干等"。

- 它的名字**没有**叫"不帮助"（那是说明书口吻）。现在用的是 **「这次，就让它过去」**，
  副标题「不消耗守护之光 · 空格」。
- 它的结算和"没来得及"**完全一致**：记一次错过（结局页的"错过 N 次"+1）、
  走这一刻的 `onTimeout` 收尾台词与数值、不算干预成功、**不消耗守护之光**。
  理由是这两件事在叙事上是同一件事：没有伸手。差别只在于"等出来的"还是"点出来的"。
- 空格 / 回车在干预窗口里也等于它（窗口刚打开的 0.7 秒除外，见 §9.1）。

**请你确认**：

- [ ] 文案就用「这次，就让它过去」（保持现状）
- [ ] 换成你写的名字：＿＿＿＿＿＿＿＿＿＿
- [ ] 结算也要改（例如"点了就不算错过"）——请说明算成什么

**② 「同学聚拢围观」那张图是白色背景的不透明图，当整幅卡片演出时中间是一大片白墙。**
交付的 `day2/common/同学聚拢围观.jpg` 是 640×480、**不含透明通道**（实测全透明像素 0%），
构图是"四个同学从四角探头看向镜头"，中间是空白。作为整舞台的"剧情卡"演出时，
画面读起来就是"四个脑袋围着一块白板"。
如果这张的原意是**叠在场景上的"围观"层**（像 D2 的两张抠图那样直接站在教室上），
那需要重出一版带透明通道的；否则现在这样就当作一张构图特殊的卡片。

- [ ] 重出带透明通道的版本（我改成"叠在场景上"渲染，代码只差一个 `alpha` 标记）
- [ ] 就这样（当作一张"从视角往上看围观同学"的卡片）

**③ 删掉了开发期的通用小人（占位人物图形）。**
此前画面里有一个 `components/student.ts` 现画的通用 SVG 小人，它不分性别、
跟正式立绘毫无关系，而且**越缺图越会出现**（真图在场时它才让位）。
现在整个删除：场上的人只可能是**正式立绘**或**剧情插画**，两个都没有时就是一间干净的空教室。
配套地，**人物类素材缺图时不再画"占位方块 + 中文名"**（只有环境类底图保留占位色块）。

**这里有一个副作用要请你确认**：策划案 §3.4 的"摸摸头"彩蛋原来是点那个小人触发的，
小人没了，我把它的落点改成了**点立绘**（心情 +2、飘一句"……有点安心。"，每一幕限一次）。
因为"点立绘"同时还是"继续下一句"，所以一次点击两件事都做 ——
好处是绝不会出现"点了没反应"的死点击，代价是摸头和翻页无法分开。

- [ ] 就这样（点立绘 = 摸头 + 翻页）
- [ ] 摸头改成不许翻页（点立绘只摸头，需要再点一下才翻页）
- [ ] 把摸头挪到别的地方：＿＿＿＿＿＿＿＿＿＿

**④ D2 08:00「成绩单来了」之后"没有人图"是个真 bug，已修（这里只是备案）。**
那一刻的干预窗口画面（同学聚拢围观）在代码里被误写成了"按性别取图"，
解析成一个不存在的资源 id，于是**整个窗口静默地没有人物图** ——
画面上只剩一间空教室。现在三层修复：引用入口用错会直接报错、
单测逐槽位对账"每个挂上去的图都真的画得出来"、真浏览器逐窗口取样"这一屏必须有人"。
**不需要你做什么**，但如果你在别处看到过"某一刻画面特别空"，请告诉我是哪一刻。

**你的决定**：＿＿＿＿＿＿＿＿＿＿＿＿

---

## 7 干预"随机成败"没有失败文案

**问题**：脚本第 138–144 行给「悄悄话」写了"命中（+3）/ 落空（−3）"两种结果，
第 362 行也写"安慰不保证生效（焦虑夜随机 ±3）"。
但**每条干预反馈台词只写了一个版本**，没有写失败时该显示什么。

也就是说：如果给"悄悄话"接上随机成败，**失败时会没有台词可以说**。

**现在临时怎么做的**：所有干预都按"必成功"处理，随机成败的字段在契约里留着但没内容用它。

**请你选一个**：

- [ ] 所有干预都必成功（保持现状，最简单）
- [ ] 只有「悄悄话」有随机成败，失败文案我补：＿＿＿＿＿＿＿＿＿＿
- [ ] 其他干预也要随机成败，失败文案我补（需要逐条给）

**你的决定**：＿＿＿＿＿＿＿＿＿＿＿＿

---

## 8 图鉴二周目差分，缺 3–5 句文本

**问题**：脚本第 350 行写「首发版本图鉴池控制在 20–30 句，**留 3–5 句做二周目差分**」。
一周目实测收到 **23 句**，**但没有任何句子是二周目才出现的** —— 因为没人写这 3–5 句。

> ⚠️ 更正一处我此前写错的结论：这里原来写的是「图鉴已按『云朵 / 悄悄话』两个子栏收好」。
> **实际上「悄悄话」子栏是空的**（内容层没有一句带 `group: 'whisper'`），
> 详见附六 C 条 —— 那是一条 bug，不只是缺文案。

技术上"二周目解锁"的条件已经做好了（游戏记录了通关次数），只差文本。

**请你给出**：

- [ ] 二周目差分台词 3–5 句（写在下面），并说明插入到哪个时刻
- [ ] 不做二周目差分

```text
1.
2.
3.
4.
5.
```

**你的决定**：＿＿＿＿＿＿＿＿＿＿＿＿

---

## 附一：其余已定稿、不用你回的部分

以下是脚本写清楚了、已按原文实现并测试过的，列出来供你核对：

| 项 | 实现值 | 依据 |
|---|---|---|
| 每日守护之光 | D1–D5 = 2 点，D6 = 3 点，D7 = 0 点且完全不能干预 | 脚本第 221、245、284 行 |
| 干预消耗 | 每次 1 点守护之光，不可购买、不可累积 | — |
| 事件窗口 | 普通事件 10 秒；D6「最后一夜」15 秒 | 脚本各时刻标注 |
| 生长结局阈值 | 平均心情 ≥42 | 脚本第 323 行 |
| 羁绊检查点 | 羁绊 ≥6 触发金色版（D5 21:00）—— 原实现漏了，本轮已补，见第 6.6 条 | 脚本第 236 行 |
| 睡眠判定 | 睡眠时长 < 7.25 小时（= 435 分钟）才触发睡眠不足 —— 原实现漏了门控，本轮已补，见第 6.5 条 | 脚本第 48 行 |
| D7 无干预 | 第 7 天一个干预按钮都没有 | 脚本第 284–286 行 |
| 结局判定优先级 | 隐藏 → 晴空 → 生长 → 雨过 | 依脚本结局表 |
| 结局页元素 | 七日云朵 + 守护星级 + 图鉴计数 + 留言瓶 | 脚本第 327 行 |

> **关于"守护星级"**：脚本只写了结局页要有"守护星级"，没给分档。现在用的是引擎兜底分档
> （羁绊 12→★★★★★、9→4 星、6→3 星、3→2 星、0→1 星）。如果这个手感不对，请给一版你要的分档。

## 附二：**没有金钱系统**（确认一下，避免误会）

有一次玩家把干预按钮上的 **「消耗 1 光」** 看成了"1 元"。这里说明一下：

- **本作没有金钱、货币、购买、付费或内购设定，也不应该加。**
- 玩家唯一的资源是**守护之光**：每天醒来给 2 点（D6 给 3 点、D7 不给），
  每次干预消耗 1 点，**不可购买、不可累积**。它代表"你今天还能护他几次"。
- 为避免再次误读，按钮副标题已改为「**消耗守护之光 ✦1**」。

如果脚本里其实设计过某种"花费"，请指出位置；否则就按"没有金钱系统"继续。

## 附三：当前实测的结局阶梯（供你核对难度手感）

| 玩法 | 结局 | 平均心情 | 羁绊 | 干预次数 | 错过 |
|---|---|---|---|---|---|
| 每步都选最暖 | 晴空 | 65.7 | 12 | 12 | 5 |
| 每步都选最冷 | 生长 | 60.8 | 12 | 12 | 5 |
| 只救前 2 天 | 雨过 | 37.6 | 4 | 4 | 13 |
| 只救前 4 天 | 生长 | 59.7 | 8 | 8 | 9 |
| 每天都把光留给夜里那次「偷加两小时」 | 笨拙的守护 | 28.8 | 6 | 6 | 11 |

内容规模：**44 个时刻（其中 D5 的「羁绊检查点」按羁绊高低走两个互斥分支，所以一周实际走 43 个）/ 其中 17 个可干预窗口 / 全周共 13 点守护之光**。
也就是说**一周必然有 4–5 个事件是拦不住的** —— 这是按脚本的守护之光上限算出来的，
如果这不符合你的设计意图，请在第 1 条的选项 B 里改光的数量。

## 附四：这一轮的表现层决定，请你确认（涉及观感和音效）

### 4.1 ✅ 已定：所有文字走底部对白框，**云朵文字容器已删除**

**这一条已经拍板并做完了，不需要你再回。** 记为已定的原因是它推翻了脚本的一处写法，
将来有人对着脚本核对时会发现"对不上"。

**脚本原文**：每个时刻的舞台提示都写的是**云朵（灰/白/金）**（脚本第 3 行甚至直接把这栏叫
「云朵台词」），而开发文档 §5.1 的表现分级里，L1 普通对白要用底部单句台词框。两套并存时，
同一句话会因为分级不同而换个样子。

**决定（2026-09）**：**全部文字走底部对白框，云朵气泡整个删掉。**

| 从前怎么分 | 现在 |
|---|---|
| L0 环境独白 → 云朵气泡 | 底部对白框 |
| L1 普通对白 → 底部对白框 | 底部对白框 |
| L2 事件反馈 → 云朵气泡 | 底部对白框 |
| L3 终章 → 云朵气泡 | 底部对白框 |

**你的台词一个字都没动**，改的只是它画在哪里。脚本标的**颜色保留下来了**：
灰 / 白 / 金现在是这句话的**语气色**（`DialogueLine.tone`），只染对白框左边那条竖线 ——
白 = 平和、灰 = 低落、金 = 心动。**没有形状了，颜色还在。**
如果你希望这个颜色更重（比如整块底色跟着变），说一声，那是 CSS 里一行的事。

**下面两处也叫「云朵」，它们不是文字气泡，我没有动**：

- **结局页的「七日云朵」** —— 脚本第 327 行点名要有的固定元素（按当日心情着色的七朵云）。
- **「云朵图鉴」** —— 图鉴的名字（脚本第 348 行「五、云朵图鉴候选句汇总」）。

这两个名字要不要跟着改？图鉴里收的句子从前都出现在云朵气泡里，叫「云朵图鉴」说得通；
现在它们出现在对白框里，叫「云朵图鉴」就只是**产品命名**了，不再是"它长什么样"。
**这是文案决定，等你一句话**（改的话我同步改标题页、结局页、图鉴页三处文案）。

### 4.2 D4「爸妈来电」的手机震动已接上

脚本 §5.2 要求这个时刻有**手机震动**的表现，但原文只写了台词、没写信号类型。
现已把这个时刻的事件信号改成 `phoneWave` —— 引擎会据此播**手机震动音**并叠加震动动画，
画面上出现的是美工交的 `signal.phone`（手机震动的水彩信号图）。
**这是唯一一处我替你选了表现类型的地方**，如果你本来想用的是别的信号（比如雨云），说一声就换回去。

### 4.4 情绪信号已经从 emoji 换成正式交付的图（本轮）

这一条**必须让你知道**，因为它是画面上"最后一批占位图案"：

从前六个情绪信号画的是**六个 emoji 字符** —— 🌧（雨云）◌（灰圈）✗（红叉）💤（困倦）
📱（手机）🌫（自定义），靠一行 `font-size: 52px` 撑大小。而美工其实早就把整套水彩信号图
交在了 `如果有你-场景-Day1/如果有你/03_signals/` 里（乌云 / 灰云 / 白云 / 金云 / 困倦碎片 /
红叉 / 孤立圆 / 手机 / 箭头 / 出口高亮，外加金色拉链与护住的手两张干预特效），
**只是从来没有 AssetId 把它们接进来**，于是一整轮里画面上都是一颗 emoji。

现在它们全部接上了（`signal.*` / `vfx.*`，见 `art/README.md`），尺寸也重新标过：
emoji 是实心剪影，水彩图有大量留白和柔边，同样的外框尺寸下后者明显更弱，
所以基准从"132px"提到了"192px（没有插画时）/ 153px（插画在场时）"。
**如果你觉得情绪信号还是太弱或者太抢，说一声，这两个数就在 `src/styles/base.css` 的 `.signal` 上。**

另外：`signal.cloud.white` / `signal.cloud.gold` 这两张目前还没有被内容引用
（`kind: 'custom'` 是留给"某一刻想用别的图"的逃生口，写 `Signal.asset` 即可）。
如果你想在某些时刻用上白云/金云（比如羁绊检查点、终章），把要用的时刻告诉我。

### 4.3 哪些动画**刻意不出声**（供 C 核对，避免以为漏配了）

音效按开发文档 §8.7 合成，共 12 种：脚步 / 地点环境 / 逐字提示 / 成功 / 错过 / 雨声 / 手机震动 / 拉链 / 糖果 / 时钟 / 拨云 / 终章和弦。
脚本里的干预动画和音效的对应关系如下 —— **有 5 个动画是故意不配音的**，不是漏了：

| 动画 | 音效 | 说明 |
|---|---|---|
| 拉链（zipMouth） | ✅ 拉链声 | |
| 糖果（candyDrop） | ✅ 糖果声 | |
| 时钟倒转（clockRewind） | ✅ 时钟声 | |
| 拨云（clearSky） | ✅ 拨云声 | |
| 手机震动（phoneBuzz） | ✅ 震动声 | D4 爸妈来电 |
| **捂耳朵（handCoverEars）** | ❌ 刻意静音 | 表现的是"世界安静下来"，配音反而破坏 |
| **暖光（warmGlow）** | ❌ 刻意静音 | 纯视觉 |
| **云散（cloudDissolve）** | ❌ 刻意静音 | 成功音效已经负责了听觉反馈 |
| **震动（shake）** | ❌ 刻意静音 | |
| **推近（zoomIn）** | ❌ 刻意静音 | |

如果「捂耳朵」你希望有一声"嗡——"的耳鸣感，请说一声，我再加。

## 附五：操作层补完 —— 键盘现在能从头走到尾（**请你知悉**）

验收提的"「前往 操场」这个按钮也要能用回车点"。这确实是全流程里**最后一处只能用鼠标**的
地方：地点门出现时，画面上的下一步不是"继续"而是"去哪"，所以它不在翻页那条链路上，
回车按下去没有任何反应。现在回车 / 空格等于点这颗按钮，按钮上也写明了「回车 / 空格」。

顺带把人物自动移动转场（宿舍走教学楼那段）也对齐了：**回车直接到终点**，
和「加速 ×1/×3」一起，省掉每一次换地点干等 3.6 秒。

现在从标题页开始，一路上**只有三处必须用鼠标（或 Tab + 回车）**，这三处都是"要你在多个
选项里挑一个"，刻意不让回车替你决定：

| 必须自己选的地方 | 为什么不让回车代替 |
|---|---|
| 标题页（继续守护 / 开始守护 / 云朵图鉴） | 三个按钮，"回车选哪个"没有正确答案 |
| 开局问卷（那时候的TA，是——） | 它决定主角立绘取男版还是女版，猜错就是整局素材换人 |
| 干预窗口里点某一个干预 | 会花掉守护之光 —— **回车在窗口里只等于「这次，就让它过去」**（不伸手），不会替你花光 |

这三处按 **Tab + 回车**都能操作（浏览器原生的"按下这颗按钮"），所以键盘用户不会卡住。
其余的推进（对白、地点门、走路转场）一路回车即可。

---

## 附六：脚本台词 vs 游戏内容对账（**请你逐条裁决**，其中 4 句是 ★ 收藏句）

**背景**：你们反复强调"一个字都不要改你的台词"。为了确认这件事做到了什么程度，
我写了一个对账脚本（`scripts/audit-script-lines.mjs`）：把脚本里**每一句「…」**抓出来，
和游戏的全部文本（`content/` + `src/`）逐句比对，列出**脚本里有、游戏里一个字都找不到**的句子。
随时可重跑：`node scripts/audit-script-lines.mjs`。

**结果**：脚本共 142 处「」引文（114 句不重复），其中 **23 句在游戏里找不到**。
逐条分完类之后，**真正需要你裁决的是 6 句**，其余都能解释清楚。

### A. 真正缺的台词（6 句，请逐条给个决定）

**A 组：脚本写了两句，游戏只做了第一句，而且把数值与 ★ 挂错了地方（4 句）**

这四处的形状完全一样：

| 时刻 | 脚本原文（行号） | 游戏里现在是什么 |
|---|---|---|
| D3 12:30 塞糖 | ★「……原来，有人记得我。」（L168，**+12，全游戏最高单次**） | 只有「糖？……给我的吗。」 |
| D4 17:00 黄昏 | ★「粉笔灰在光里飘。好像，也没那么糟。」（L200，+3） | 只有「黄昏的教室，是橘色的。」（★ 与 +3 被挂在了这一句上） |
| D5 10:00 同桌推笔记 | ★「什么也没说。可是，好像什么都说了。」（L228，+8） | 只有「同桌把笔记推了过来，什么也没说。」（+8 挂在它身上） |
| D5 21:00 羁绊检查点 | ★「总觉得，最近运气有点好。」（L237） | 只有「像是有谁，一直在。」（+5 在它身上，脚本把 +5 给的是第二句） |

**为什么这不是我随手加的**：脚本在这四处都是"先一句陈述、再一句点题（带 ★ 与数值）"，
我这边只落了陈述句。**后果是图鉴里少了 4 句最打动人的话**，
而且数值落在了"不是 ★ 的那一句"上。

请选（可以逐条不同）：

- [ ] 补上第二句，数值与 ★ 都归第二句（最贴脚本）
- [ ] 补上第二句，但两句都收进图鉴
- [ ] 不补，就保持现在这一句（那我至少把 ★ 与数值挪到对的位置上）
- [ ] 逐条说明：＿＿＿＿＿＿＿＿＿＿

**B 组：另外两句（2 句）**

- **D6 07:30 月考**：脚本给了**两版** ——【补课版】「今天{补课日}。可是，要补课。」（L248）/
  【不补课版】「难得的休息日。可是，月考。」（L249）。游戏里只有不补课版，而且是写死的。
  `{补课日}` 这个变量还在（作息定制的默认值），所以技术上能按它分流。
  - [ ] 按 `{补课日}` 分流，两版都写进去
  - [ ] 就用现在这一版（问卷已经砍了作息题，补课日不再是玩家能改的东西）
- **D7 终章**：「……好像，一直有人在我身边。」（L305）。游戏里的终章末句用的是
  §六 修改记录里定稿的「感谢你，撑过来了。」（那一句在）。
  - [ ] 这一句是 V1.1 之前的旧版，不用补（我按这个理解处理，标注在此备案）
  - [ ] 要补：请说明插在哪一句之后

### B. 能解释清楚、不用你回的 17 句

| 类别 | 句子 | 为什么不在 |
|---|---|---|
| 被砍掉的开局问卷变量 | `{女生/男生}。{短发/长发/马尾}。{校服配色}。`、`{闹钟}起床。{熄灯}熄灯。…` | 问卷只剩性别一问（见 §6.7），这些变量已不存在 |
| V1.1 修改记录（旧措辞） | `马尾`、`上课打盹`、`机械跑步`、`考试失利`、`独自吃饭`、`都是你，对吧。`、`谢谢你。`、`我撑过来了。`、`谢谢你守护我。` | 都是"改成什么"的**记录**，不是要实现的台词（末句改为「感谢你，撑过来了。」，已实现） |
| 〇、文风基准里的举例 | `守护我`、`接住`、`什么也没说。可是，好像什么都说了。`、`不知道为什么，今天精神特别好。` | 文风示例；其中「什么也没说…」同时是 D5 的真实台词，已列在 A 组 |
| 结局页模板 | `来自未来的你：『……』` | 已实现为 `来自未来的你：<q>…</q>`（引号样式不同，见结局页留言瓶） |
| 章节名 | `冒雨跑操` | 是脚本的章节标题，游戏里对应时刻的标题叫「跑操」 |

### C. 顺带发现：图鉴的「悄悄话」子栏**是空的**（这是 bug，不是文案问题）

配置里声明了两个子栏（`galleryGroups: { '': '云朵', whisper: '悄悄话' }`），
脚本第五章也要求「悄悄话」子栏单独收玩家说出的金色悄悄话（L350 点名三句）。
但**内容层没有任何一句台词带 `group: 'whisper'`** —— 三句悄悄话
（「宝贝，没关系的…」「祝你好梦。」「你已经做得很棒了。」）都被收进了默认的「云朵」栏，
其中两句甚至只作为**按钮文字**存在、被收藏的是反馈台词「……好像，真的安心了一点。」

后果：图鉴里**永远只会出现「云朵」一栏**，「悄悄话」子栏从来不会渲染出来。

> 注意：这条与 §8（二周目差分缺 3–5 句）是同一片区域，所以我没有擅自改。
> 修法是给那三句悄悄话的反馈台词加 `group: 'whisper'`（一行数据），并把
> 「祝你好梦。」「你已经做得很棒了。」这两句**被收藏的文本**从"反馈台词"改成"悄悄话本身"。

- [ ] 按上面这样修（悄悄话收玩家说的那句话本身）
- [ ] 保留现在的收法，只把它们挪进「悄悄话」栏
- [ ] 干脆去掉「悄悄话」子栏（那我把配置里那一项也删掉，不留死配置）
