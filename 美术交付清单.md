# 美术交付清单

> 给美工 A / 美工 B。看完这份就能开始交图，不需要读代码。
> 交完图请把这份清单里的「状态」勾一下，方便对账。

---

## 一、怎么交图（三步）

### 1. 把图按**固定文件名**放进 `art/` 目录

文件名必须是下面表里的 **AssetId**，扩展名用 `.jpg` / `.png` / `.webp`（也能收 `.svg`）。
例如卧室基底就存成 `art/map.bedroom.background.jpg`。

**交来的文件如果本来不叫 AssetId、尺寸也是原生出图尺寸**（例如 `bg_bedroom_morning.png` 1672×941），
那是对的 —— 不要自己改名、也不要自己缩放，交给框架同学跑导入脚本统一转换：
`scripts/import-scene-art.ps1`（环境图）、`scripts/import-story-art.py`（D2 起的剧情图）。
两个脚本都会打印每张图的缩放偏差与转换前后的体积，偏差大就说明比例要重新商量。

**D2 起按天交来的剧情图**（`如果有你_DAY<N>人物素材_可发送_v*`）不需要你自己起 AssetId 名：
按交付包里的中文文件名交就行，导入脚本的映射表负责落成 `art.story.<boy|girl>.<name>`，
登记表在 `content/storyArt.ts`（哪张图落在哪一刻写在那里）。
**有两点是硬要求**：

1. **男女主角必须各一套**，文件名里带 `男生_` / `女生_`，运行时只加载玩家选的那一套，
   任何一张剧情图里都不得同时出现男女主角（说明书的 §一.2）。
   老师 / 同学这类共用的图放 `common/` 或 `teacher/`。
2. **透明抠图和带背景的整幅图都可以交，但要说清楚是哪一种**。
   两者在程序里的渲染方式不同：整幅图按"剧情卡"演出（带卡片描边与模糊底），
   抠图则直接站在场景上（不画卡片）。判定只有一条依据 —— 交付时告知，
   我们会写进 `content/art.spec.json` 的 `alpha` 字段；
   **不要指望程序靠扩展名猜**：D1 那批"transparent/"的 PNG 其实全是全不透明的整幅图。

### 2. 跑一次校验

```bash
npm run art:check
```

它会检查文件名对不对、尺寸对不对、单张是否超过 1.2 MB。
**名字或尺寸不对会直接报错并说明错在哪**，这是故意的 —— 防止错尺寸的图被静默拉伸。
`npm run build` 和 `npm test` 都会自动先跑这一步。

### 3. 让框架同学跑一遍验证

```bash
npm run build && npm run preview      # 一个窗口
npm run test:browser                  # 另一个窗口，会截图到 tests/browser/shots/
```

---

## 二、要交的图

### 环境（美工 A）

| # | AssetId（=文件名） | 尺寸 | 用途 | 状态 |
|---|---|---|---|---|
| 1 | `map.bedroom.background` | **1280 × 720** | 卧室基底：调试入口底图 / 无时相时的卧室 | ✅ 已交 |
| 2 | `map.bedroom.morning` | **1280 × 720** | 卧室·清晨（D1 06:20 黎明） | ✅ 已交 |
| 3 | `map.bedroom.night` | **1280 × 720** | 卧室·夜灯（睡前） | ✅ 已交 |
| 4 | `map.bedroom.lightsOut` | **1280 × 720** | 卧室·熄灯（睡眠不足） | ✅ 已交 |
| 5 | `map.classroom.background` | **1280 × 720** | 教室基底（叠加层之下的那一张） | ✅ 已交 |
| 6 | `map.playground.background` | **1280 × 720** | 操场（当前用的是 D1 跑操那张） | ⚠️ 临时顶替 |
| 7 | `overlay.classroom.morning` | **1280 × 720** | 教室·清晨光（**必须透明**） | ✅ 已交 |
| 8 | `overlay.classroom.night` | **1280 × 720** | 教室·夜间光（**必须透明**） | ✅ 已交 |
| 9 | `overlay.rain` | **1280 × 720** | 雨幕叠加层（**必须透明**） | ✅ 已交 |
| 10 | 操场的中性底图 | **1280 × 720** | 换掉 6 号临时顶替的那张 | ☐ |
| 11 | `map.classroom.foggy` | **1280 × 720** | 教室·下午隔着雾（D4 14:00） | ✅ 已交 |
| 12 | `map.classroom.dusk` | **1280 × 720** | 教室·黄昏粉笔灰（D4 17:00） | ✅ 已交 |
| 13 | `map.classroom.earlyLight` | **1280 × 720** | 教室·天亮得早一点（D5 07:40） | ✅ 已交 |
| 14 | `map.playground.gentle` | **1280 × 720** | 操场·温柔的风（D5 16:30，**不带雨**） | ✅ 已交 |
| 15 | `map.classroom.makeup` | **1280 × 720** | 教室·补课清晨（D6 07:30 / 08:30） | ✅ 已交 |
| 16 | `map.playground.rainRun` | **1280 × 720** | 操场·考后冒雨（D6 15:00，雨**画在图里**，程序不再叠雨幕） | ✅ 已交 |
| 17 | `map.playground.clearing` | **1280 × 720** | 操场·雨后提前放晴（D6 15:00 干预成功时切过去） | ✅ 已交 |
| 18 | `map.playground.blueSky` | **1280 × 720** | 操场·今天的天好蓝（D7 07:50） | ✅ 已交 |
| 19 | `map.classroom.penSound` | **1280 × 720** | 教室·笔尖沙沙（D7 10:30） | ✅ 已交 |
| 20 | `map.classroom.finale` | **1280 × 720** | 终章·抬头背景（D7 15:00，与主角抬头立绘组合） | ✅ 已交 |
| 21 | `ending.memoryEcho` | **1280 × 720** | 记忆回响·黑屏（终章推近之后那一段） | ✅ 已交 |

**⚠️ 6 号现在是"D1 跑操"那张图在顶替操场底图。** 它出现在地点调试入口上，
也出现在所有操场的时刻里。请交一张**中性的操场底图**来换掉它（不要改 id，只换文件）。
注意 14 / 16 / 17 / 18 号那四张操场图**不能**拿来顶替它 —— 它们各有各的天气与时段，
而 6 号要的是"哪个时段都能用"的那一张。

**旧清单里 11 号"教室的白天/黄昏光"这一项取消**：D2 之后教室的时段变化改用
**整张独立空镜**（11–13、15、19、20 号），不再走"一张底图 + 一层光"。
教室基底（5 号）与两层光（7、8 号）继续用于 D1 与没有专门空镜的那些时刻。

### 立绘与结局（美工 B）

| # | AssetId（=文件名） | 尺寸 | 用途 | 状态 |
|---|---|---|---|---|
| 22 | `portrait.player` | **320 × 420** | 主角立绘（未定性别时的兜底） | ☐ |
| 23 | `portrait.player.boy` | **320 × 420** | 男生主角 | ✅ 已交 |
| 24 | `portrait.player.girl` | **320 × 420** | 女生主角 | ✅ 已交 |
| 25 | `portrait.player.boy.rest` | **320 × 420** | 男生·放松态 | ✅ 已交 |
| 26 | `portrait.player.girl.rest` | **320 × 420** | 女生·放松态 | ✅ 已交 |
| 27 | `portrait.teacher` | **320 × 420** | 老师·普通站立（男女共用） | ✅ 已交 |
| 28 | `portrait.teacher.strict` | **320 × 420** | 老师·严肃批评 | ✅ 已交 |
| 29 | `portrait.npc` | **320 × 420** | NPC 立绘（当前无台词） | ☐ |
| 30 | `ending.card` | **1280 × 720** | 结局页背景（四个结局共用）= D7 的「结局页_七朵云背景」 | ✅ 已交 |

**不需要一次交齐。** 没交的会自动用占位图，游戏照常能跑能测。可以一张一张替换。

**`portrait.npc` 与 `portrait.player` 现在不用交了**（29 号与 22 号）：
开局问卷只剩性别一题，主角立绘只有 `boy` / `girl` 两套（各带一张放松态）；
`portrait.npc` 那张通用 NPC 位过去是屏幕右下角那块"NPC｜占位立绘"的来源，已经不再上屏。
老师只在 D2 10:20「拖堂说教」与 D3 10:00「再次被批评」两刻出场，用 `portrait.teacher[.strict]`。

**30 号已经用 D7 交付的七朵云背景顶上去了。** 顺便修掉了一个一直没被发现的坑：
结局场景过去只写了场景 id、没写底图，而 `runEnding` 拿的是结局**自带**的那份场景，
所以结局画面其实一直没有底图（只有兜底渐变）。接 D7 素材时才发现，现在有单测盯着。

### D2 起的剧情图（`art.story.*`，按天交）

**按天交来的 `如果有你_DAY<N>人物素材_可发送_v*` 包里的中文文件名就是交付形态**，
导入脚本负责落成 AssetId，登记表在 `content/storyArt.ts`。已接入的部分：

| 交付文件 | AssetId | 尺寸 | 落在哪一刻 | 状态 |
|---|---|---|---|---|
| `day2/boy·girl/…_查看成绩单` | `art.story.<boy\|girl>.report` | **480 × 640** | D2 08:00 成绩单来了 | ✅ 已接 |
| `day2/teacher/老师_递出成绩单.png` | `art.story.teacher.handout` | **480 × 640** | 同上（右侧第二张） | ✅ 已接 |
| `day2/common/同学_聚拢围观` | `art.story.common.crowd` | **640 × 480** | 同上（干预窗口） | ✅ 已接 |
| `day2/boy·girl/…_上下铺皱眉睡觉` | `art.story.<boy\|girl>.bunkSleep` | **480 × 640** | D2 06:20 赖床 / D2 23:30、D3 23:40 睡眠不足 | ✅ 已接 |
| `day2/boy·girl/…_食堂吃饭` | `art.story.<boy\|girl>.canteen` | **640 × 480** | D2 12:40 | ✅ 已接 |
| `day3/boy·girl/…_晨读喝豆浆` | `art.story.<boy\|girl>.morningRead` | **640 × 480** | D3 07:50 晨读 | ✅ 已接 |
| `day3/boy·girl/…_一个人的午饭` | `art.story.<boy\|girl>.lunchAlone` | **640 × 480** | D3 12:30 | ✅ 已接 |
| `day3/common/操场_风把云吹得很快` | `map.playground.windy` | **1280 × 720** | D3 15:40「风」（**场景底图**，不是剧情卡） | ✅ 已接 |
| `reuse_DAY*/…_紧绷状态` | 复用已交的 `art.d1.<boy\|girl>.tense` | 480 × 640 | D2 10:20 拖堂说教 / D2 22:50 焦虑 / D3 10:00 | ✅ 复用 |
| `reuse_DAY*/…_疲惫状态` | 复用 `art.d1.<boy\|girl>.tired` | 480 × 640 | D2 16:00 体育课改自习 | ✅ 复用 |
| `reuse_DAY*/…_正常坐姿·放松坐姿` | 复用 `art.d1.<boy\|girl>.sit / .sitRelax` | 480 × 640 | 晚自习、午休 | ✅ 复用 |
| `reuse_DAY*/…_眼神放空` | 复用 `art.d1.<boy\|girl>.relax` | 480 × 640 | D4 14:00「下午隔着雾」叠在教室空镜上 | ✅ 复用 |

**D4–D7 已接入的部分**（`npm run art:check` 现在报 **80/85 张已交付**）：

| 交付文件 | AssetId | 尺寸 | 落在哪一刻 | 状态 |
|---|---|---|---|---|
| `day4/…_梦里都在考试` | `art.story.<boy\|girl>.examDream` | **480 × 640** | D4 06:10 清晨 | ✅ 已接 |
| `day4/…_排名被当众念出` | `art.story.<boy\|girl>.rankCalled` | **640 × 480** | D4 09:30（干预窗口沿用同一张） | ✅ 已接 |
| `day4/…_家长来电低头` | `art.story.<boy\|girl>.phoneBow` | **640 × 480** | D4 20:00 左侧 | ✅ 已接 |
| `day4/common/手机_家长来电` | `art.story.common.phoneCall` | **512 × 512** | D4 20:00 右侧（**1:1 道具特写**） | ✅ 已接 |
| `day5/…_同桌把笔记推过来` | `art.story.<boy\|girl>.notesPassed` | **640 × 480** | D5 10:00 左侧 | ✅ 已接 |
| `day5/common/课堂笔记_道具` | `art.story.common.notesProp` | **512 × 512** | D5 10:00 右侧第二张 | ✅ 已接 |
| `day5/common/冰汽水_道具` | `art.story.common.soda` | **512 × 512** | D5 13:00（这张本身就是那一刻的主角） | ✅ 已接 |
| `day5/…_羁绊检查点` | `art.story.<boy\|girl>.bondCheck` | **640 × 480** | D5 21:00（达标 / 没达标两个分支共用） | ✅ 已接 |
| `day6/…_监考压力捂耳朵` | `art.story.<boy\|girl>.invigilate` | **640 × 480** | D6 08:30 左侧 | ✅ 已接 |
| `day6/common/试卷与铅笔_道具` | `art.story.common.examPaper` | **512 × 512** | D6 08:30 干预窗口第二张 | ✅ 已接 |
| `day6/common/教室_饭有点凉` | `art.story.common.coldLunch` | **640 × 480** | D6 12:00（源图是 4:3，当插画卡接） | ✅ 已接 |
| `day6/…_排名公布` | `art.story.<boy\|girl>.rankPost` | **640 × 480** | D6 20:30 | ✅ 已接 |
| `day6/…_最后一夜睡眠不足` | `art.story.<boy\|girl>.lastNight` | **480 × 640** | D6 23:59（15 秒窗口） | ✅ 已接 |
| `day7/…_终章抬头` | `art.story.<boy\|girl>.finalLookUp` | **480 × 640** | D7 15:00 终章（L3） | ✅ 已接 |
| `day7/common/黑屏_记忆回响背景` | `ending.memoryEcho` | **1280 × 720** | 终章推近之后、结局台词之前 | ✅ 已接 |
| `day7/common/结局页_七朵云背景` | `ending.card` | **1280 × 720** | 结局页背景 | ✅ 已接 |
| `day3–day7/common/…空镜` | `map.*`（见环境表 11–21 号） | **1280 × 720** | 每天那个时段的整屏底图 | ✅ 已接 |

**尺寸从哪来的**：4:3 的图接成 **640 × 480**、3:4 的接成 **480 × 640**、16:9 的空镜接成
**1280 × 720**、1:1 的道具接成 **512 × 512**。都是按"它在游戏里实际显示多大"定的，
不是随手缩的（剧情卡高度上限约 572px，所以这些尺寸基本就是 1:1）。
交原图时**不要自己缩放**，导入脚本会按上面的目标尺寸重采样并在偏差 >0.5% 时报错。

**"复用"的图不要重复交** —— 说明书里那些 `reuse_DAY*精选/` 的实质就是前几天已交的同一张图
（实测 DAY2 包里 24 个文件与前几天的文件**字节完全相同**），程序按同一张 AssetId 引用。
`…_眼神放空` 与 D1 的"放松状态"也是**同一个文件**（字节相同），所以复用 `art.d1.<who>.relax`。
`reuse_*/player_move/` 那套走帧这次仍然不接（移动功能按计划最后再做）。

**两张已知形态不一致的图**（需要美工统一，见《待确认问题》6.8）：
`女生_查看成绩单.png` 与 `老师_递出成绩单.png` 是**透明抠图**（四角 alpha=0），
而同一刻男生那张 `男生_查看成绩单.jpg` 是**带背景的整幅图**。
程序按 `alpha` 字段分别渲染（抠图不画卡片描边），但两条路线的观感会不一样。
D4–D7 交来的图**全部是带背景的整幅图**（没有一张抠图），所以这个不一致只存在于 D2 那一刻。

**还缺两张 / 一张要重出**（AssetId 与尺寸已在 `content/storyArt.ts` 和 `content/art.spec.json`
里预留好，交来文件即可直接接）：

| AssetId | 尺寸 | 需求 | 状态 |
|---|---|---|---|
| `art.story.boy.lookUp` / `art.story.girl.lookUp` | **480 × 640** | D4 17:00「黄昏粉笔灰」：说明书写了要 `男生_抬头_复用.png` / `女生_抬头_复用.png`，包里没有这两个文件 | ☐ |
| `art.story.common.rankProjection` | **1280 × 720** | D6 20:30 排名公布的投影层。交来的 `排名公布_投影道具.jpg` 里**男生在左、女生在右同框**，违反"男女不得同框"，所以没有接入；需要一张"空教室 + 幕布上的名次"（或男女各裁一版） | ☐ |

**关于水印**：D4–D7 交来的 JPG 右下角普遍带生成平台的「AI生成」角标（结局页那张在底部中间）。
说明书写的是"正式接入前由美术清理或裁切"，目前**没有做程序遮盖**，验收时请一并看这件事。

---

## 三、尺寸为什么是这些

### 整屏舞台图 = 1280 × 720（16:9）

**地点背景、天气/光线叠加层、结局背景是同一类东西**：都铺满整个游戏舞台，共用一个基准。

舞台是用 `background-size: cover` 渲染的 —— 图的比例和舞台差得越多，被裁掉的部分越多。
交来的环境图原生是 **1672 × 941 ≈ 16:9**，换算到 1280 × 720 的横纵比偏差只有 **0.053%**
（亚像素级）：既不用裁画面，也不用拉伸。换成别的比例就必然要牺牲其中一个。

所以请**按 16:9 作画，并且不要自己裁**。如果画面被浏览器窗口拉宽，游戏会以中心为基准裁掉多余部分。

> 后期做人物移动时，需要你额外提供**可行走区域**（地面范围）和**出入口位置**，
> 也用同一套坐标标注（基准就是背景图的交付尺寸，代码直接从规格表读，不会两处各写一个数）。
> 现在不用交。

### 底图用 JPEG，叠加层用 PNG

- 不需要透明通道的**一律按 JPEG 交**。同一画面下 JPEG 比 PNG 小一个数量级：
  交来的源 PNG 是 1.7–2.5 MB/张，远超过单文件 1.2 MB 的上限；转成 JPEG 后是 100–170 KB。
- 叠加层**必须带透明通道**（PNG / WebP）。交给 JPEG 会把整个场景压掉。

### 叠加层的浓度 = 图自己的 alpha，游戏不会再压暗

以前游戏会把叠加层再乘 0.42，现在**不会了**。实测 `overlay.classroom.night` 平均 alpha
只有 66/255（最浓处 106），再乘 0.42 等于把浓度砍掉一半多，夜晚根本看不出是夜晚。

所以：**想多浓就画多浓**，alpha 是多少就按多少混合。
- 雨幕：细线、低 alpha、大面积透明是对的（现在最浓处 106）。
- 夜间光：整体压暗、几乎不留全透明区是对的（现在 28–105）。
- 清晨光：非常薄的冷色光是**故意**的（平均 alpha 18.5）—— 但请确认这个浓度在正式底图上看得出差别，
  看不出来的话要加浓。

### 立绘 = 320 × 420（约 3:4）

- **透明背景**（PNG / WebP 带 alpha），不要带白底或渐变底。
- 半身构图。人物**不要留大片空白**，否则缩小到 96px 宽时会显得很小。
- 游戏里用 `object-fit: contain` 渲染，所以**整张图都会显示，不会被裁**。
- **一张图同时要能当"亮"和"暗"两态用**：当前说话者会放大到 1.12 倍并保持 100% 亮度，
  另一方会压暗到 55% 亮度（注意：压暗走的是 `filter: brightness`，不是整体淡出，
  所以不会出现"半透明"的观感，但大面积死黑压暗后仍会糊成一团，请避免）。
- 主角立绘目前是**没有表情分档的**：脚本里主角是"现在的你"，视角上不说话。
  如果后续要分档，需要先和我们确认再出图。

---

## 四、时相：同一个房间的不同时段

同一天里 06:20 的卧室和 23:20 的卧室不该是同一张画面，早读的教室和晚自习的教室也不该。
所以卧室要**三张独立底图**（清晨 / 夜灯 / 熄灯），教室分两种情况：

- **D1 与没有专门空镜的时刻**：教室用**一张底图 + 一层光**。教室只有一张底图，是因为
  **教室的陈设不能因为换了时段就对不上**，而且你要维护的底图也只有一张 —— 加一个时段只是多画一层光。
- **D2 起按天交来的空镜**（11–13、15、19、20 号）：直接就是一张**独立的整屏画面**，
  不再叠光。它们各自的构图就不同（比如 D4 的"黄昏粉笔灰"是橘色的夕阳教室），
  这是交付时按天给的，程序只负责在正确的时刻换上去。
- 操场分成"跑操（不下雨）"和"雨天"两种，另外按天又交了 14 / 17 / 18 号三张不同天气与光线的操场。
  **D6 15:00「考后冒雨」的雨是画在图里的，所以那一刻程序不再叠雨幕** ——
  而干预成功会换成 17 号「雨后提前放晴」那张（交付说明 §四.3 要求的"两张背景按结果切换"）。

**D1–D7 的全部 43 个时刻现在都接上了各自该有的画面**，没有"某天偏平"的遗留。
`node scripts/art.mjs list` 可以打印每个 AssetId 的尺寸与用途。

---

## 五、现在长什么样（可以先看这些截图）

跑一次 `npm run test:browser`，截图会生成在 `tests/browser/shots/`：

| 截图 | 看什么 |
|---|---|
| `01-title.png` | 标题页 |
| `02-debug-gate.png` | 地点调试入口（背景就是该地点的正式底图） |
| `04-cloud.png` | L0 云朵独白（环境旁白） |
| `05-intervention.png` | 干预窗口（两个按钮 + 情绪信号图标） |
| `06-dialogue-box.png` | **L1 普通对白：底部对白框 + 左右立绘** ← 立绘就看这张 |
| `07-ending.png` | 结局页（星级 / 七朵云 / 留言瓶） |
| `08-mobile-ending.png` | 移动端 768px |
| `09-ending-corner.png` | 结局左上角地点名 |
| `11-scene-classroom-night.png` | **晚自习的教室**（底图 + 夜间光叠加层） |
| `12-scene-bedroom-dawn.png` | **D1 06:20 黎明的卧室** |

---

## 六、想先演练一遍替换流程

不用等真图做好，可以先跑一次演练：

```bash
node scripts/art.mjs drill
```

它会生成一套**尺寸完全合规的条纹假图**放进 `art/`，跑一遍校验，然后提示你执行
build / test / test:browser。跑完假图会自动删除，回到占位图状态。

这样你能提前看到自己的图会以什么比例、什么位置出现在游戏里。

---

## 七、不要改的东西

你**只需要替换图片文件本身**。以下任何一项都不要动，否则会破坏存档兼容和剧情顺序：

- ❌ 不要改 AssetId（文件名）
- ❌ 不要改 `content/art.spec.json` 里的尺寸基准（要改先和框架同学确认）
- ❌ 不要改剧情节点 id、`onceKey`、触发顺序、场景 id
- ❌ 不要改 `content/` 下的台词与数值
- ❌ 不要改 `src/` 下的任何代码

---

## 八、音频（还没有资源，先不用交）

游戏目前只有 Web Audio 合成的提示音（打字音、干预成功音）。
脚本里提到过环境音、脚步和关键音效，但**现在还没有定音效清单**，
等策划确认后再单独给你一份音频清单。技术侧已经留好了接口（`assets.audio`）。
