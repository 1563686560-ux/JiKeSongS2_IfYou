# 《如果有你》游戏开发文档

> **唯一开发依据：** `如果有你-完整策划案-黑客松Markdown版V1.2.md`  
> **负责人：** A（游戏框架与实际实现）  
> **技术基线：** TypeScript + DOM/CSS/SVG + Web Audio API  
> **明确不使用：** Phaser、Unity、Godot 或其他游戏引擎

本文不继承此前策划案中的玩法设计。剧情顺序、地点、时刻、事件、干预、数值和文本均按上述 V1.2 执行案组织；开发文档只定义实现方案，不自行扩写剧情。

---

## 1. 项目目标与边界

### 1.1 产品目标

两天内完成一个可玩的七日流程 Demo。当前功能优先保证“地点按钮触发剧情”的完整链路；人物移动控制属于后期增强，不纳入本阶段核心实现。最终体验仍然必须按 D1 → D7 逐天推进，并在第 7 天终章后进入结局：

```text
日初 → 显示下一目标地点 → 点击目标地点按钮
→ 地点过渡 → 普通对白/环境独白/特殊动画（都走底部对白框）
→ 干预或 miss → 保存状态 → 下一个目标
→ 当天剧情完成 → 下一天 → 第 7 天终章 → 结局
```

### 1.2 必须实现

- 卧室、教室、操场三张场景地图占位图；
- 固定路线、目标箭头、地点名和目标提示；
- 当前阶段使用目标地点按钮触发剧情，PC 与移动端均可点击；
- D1 → D7 严格按天、按内容顺序推进，不允许跳天或自由跳转；
- 到达/选择目标地点后触发一次性剧情节点；
- 人物自由移动控制列为后期增强，不影响当前剧情功能验收；
- L0 环境独白、L1 普通对白、L2 特殊动画、L3 终章；
- 心情、羁绊、守护之光、干预窗口和 miss；
- 七日流程、存档恢复、结局页、七朵云、图鉴和留言瓶入口；
- 环境、反馈、雨声和终章和弦等音频能力；脚步声接口可以预留，但人物移动未实现前不作为当前验收项。

### 1.3 不做

- 当前阶段不做人物自由移动控制；后期如需加入，仅作为增强，不改变剧情节点接口；
- 当前阶段不做碰撞、物理、寻路或虚拟摇杆；
- 不做开放世界、战斗、复杂任务树；
- 不新增 V1.2 之外的人物关系、支线和对白；
- 食堂和小卖部不单独制作地图；
- 不制作完整过场电影，特殊剧情只做短动画；
- 不做完整角色定制动画；
- 不引入游戏引擎；
- 序列帧系统只有在美术明确交付格式后才扩展。

---

## 2. 技术方案

| 层 | 实现 |
|---|---|
| 语言 | TypeScript 5，`strict: true` |
| 渲染 | 原生 DOM |
| 画面 | CSS、SVG；正式背景可用 WebP/PNG |
| 动画 | CSS Animation、Transition、Web Animations API |
| 音频 | Web Audio API + 可选音频文件 |
| 存档 | `localStorage` |
| 构建 | Vite 5 + `vite-plugin-singlefile` |
| 测试 | Vitest + jsdom；Playwright 为后续增强 |
| 交付 | 单文件 `dist/index.html` |

V1.2 当前阶段的核心是固定路线、目标地点按钮、剧情触发、逐句对白、短动画和状态结算。人物移动控制、碰撞和物理模拟明确放到后期，不作为本阶段前置条件。DOM/CSS/SVG 可以直接完成地点按钮、底部对白框、立绘切换、响应式界面和单 HTML 打包，因此不使用游戏引擎。

运行模型为事件驱动状态机，不建立常驻 60fps 循环：

```text
加载 GameConfig → 读取 GameState → 执行节点
→ 等待地点点击/对白/动画 Promise → 结算并存档 → 下一节点
```

---

## 3. 架构与目录

### 3.1 引擎与内容分离

```text
content/（B/C 内容层）
  └─ 只引用 src/types/content.ts 的类型

src/（A 引擎层）
  └─ 读取 content/index.ts 导出的 GameConfig
```

规则：

1. `src/` 不写具体剧情文本；
2. `content/` 不写 DOM 操作和渲染逻辑；
3. `src/types/content.ts` 是唯一合同；
4. 契约变化必须同步内容、引擎、测试并递增内容版本。

### 3.2 项目结构

```text
JiKeSongS2/
├─ 如果有你-完整策划案-黑客松Markdown版V1.2.md  # 唯一依据
├─ 如果有你-剧情脚本V1.md                       # V1.2 指向的内容源
├─ 游戏开发文档.md
├─ package.json / tsconfig.json / vite.config.ts
├─ content/
│  ├─ index.ts / assets.ts / map.ts / scenes.ts
│  ├─ moments.ts / flow.ts / conditions.ts
│  ├─ dilemmas.ts（引擎能力示例，正式七日内容不走这里）
│  ├─ endings.ts / customization.ts
├─ src/
│  ├─ main.ts / types/content.ts
│  ├─ core/engine.ts / runtime.ts / util.ts / eventBus.ts
│  ├─ systems/sceneManager.ts / mapSystem.ts
│  │  dialogueSystem.ts / choiceSystem.ts / animationSystem.ts
│  │  audioSystem.ts / uiSystem.ts / assetLoader.ts / dialoguePrefs.ts
│  │  cloudSystem.ts / effectRunner.ts / gender.ts
│  └─ styles/base.css（场景里没有人图层：人物只由立绘行或剧情插画承担）
└─ tests/unit/ + tests/smoke/
```

---

## 4. 核心剧情状态机与地点入口

### 4.1 状态机

当前阶段使用地点按钮触发，状态机为：

```text
DAY_INIT → NEXT_TARGET → LOCATION_SELECT（地点调试入口）
→ LOCATION_TRANSITION（约 1 秒）→ MOMENT_PREP
→ DIALOGUE 或 CINEMATIC
→ INTERVENTION_WINDOW（仅事件）
→ MOMENT_RESULT → SAVE → NEXT_TARGET → DAY_END
```

地点按钮只作为当前开发阶段的临时调试入口，用于快速验证剧情节点、存档和 D1 → D7 顺序；它不是正式玩法，不得作为最终玩法验收。对白、动画和选择期间锁定地点入口。后期加入人物移动时，地点按钮调试入口仍可保留，但正式体验改为人物移动触发；不得改变后续剧情节点和 D1 → D7 顺序。事件窗口从地点过渡与事件铺垫结束、干预按钮出现后开始计时。

### 4.2 地点

| 地点 ID | 用途 |
|---|---|
| `bedroom` | 清晨、睡前、睡眠不足、终章前后 |
| `classroom` | 早读、上课、午休、晚自习、成绩单、排名、家长来电 |
| `playground` | 跑操、雨天、黄昏、考后冒雨 |

- 当前阶段保留固定路线的目标地点按钮，仅作为开发调试入口；
- 调试入口显示箭头、名称和可点击按钮，方便快速跳转到当前允许节点；
- 入口卡片上不再有「开发调试入口 · 非正式玩法」这枚标签（2026-09 按需求从画面上删除）：
  卡片里只剩「下一目标 / 地点名 / 时间」与那颗「前往 X」按钮。"这是调试入口、不是正式玩法"
  这件事不再写在画面上，只由本节和测试记录 —— jsdom 与真机各有一条断言守着"标签不许被加回来"；
- 点击调试入口后播放约 1 秒地点过渡；
- 调试入口不能代表正式玩法，不能替代后期人物移动验收；
- 同一地点连续时刻按时间顺序触发，不重复要求再次选择地点；
- 节点完成写入 `onceKey`，再次进入不重复播放；
- 人物移动和入口/出口素材属于后期增强；本项目不增加碰撞系统。

### 4.3 七日路线

| 天 | 路线 | 关键触发 |
|---|---|---|
| D1 | 卧室→教室→操场→教室→卧室 | 08:10 批评、15:30 跑操、23:20 睡眠不足 |
| D2 | 卧室→教室→卧室 | 成绩单、拖堂、体育课改自习、焦虑、睡眠不足 |
| D3 | 卧室→教室→操场→教室→卧室 | 再次批评、一个人的午饭、睡眠不足 |
| D4 | 卧室→教室→卧室 | 排名、家长来电、深夜睡眠不足 |
| D5 | 卧室→教室→操场→教室→卧室 | 同桌善意、羁绊检查、睡眠不足 |
| D6 | 卧室→教室→操场→教室→卧室 | 监考、考后冒雨、排名公布、最后一夜 |
| D7 | 卧室→操场→教室→终章 | 07:50、10:30 日常时刻、15:00 终章 |

### 4.4 到达规则

每个时刻转换为一个 `Moment`。到达检查顺序：

1. 天数正确；
2. 地点正确；
3. 已到触发时间；
4. `onceKey` 未完成；
5. 没有更高优先级特殊事件。

早到时显示环境循环或短句；正常或窗口内晚到正常触发；超过到达窗口使用 V1.2 内容提供的 miss 文本和数值，并记录 `missReason=late_arrival`。到达窗口与干预窗口分别计算。

---

## 5. 表现分级

> **2026-09 变更（验收反馈）**：**云朵文字容器已删除**，所有文字一律走底部对白框。
> 下表保留分级本身（它是脚本与本文档之间的对照表，也仍然记在 `presentation` 字段上），
> 但"画在哪里"这一列已经全部并成一处。脚本给每句标的「云朵（灰/白/金）」颜色
> 保留为**语气色 `tone`**，只染对白框左边那条竖线。

| 级别 | 内容 | 画在哪里（现在） |
|---|---|---|
| L0 环境独白 | 场景、主角立绘、旁白/独白 | 底部对白框 |
| L1 普通对白 | 主角/NPC 立绘、说话者高亮 | 底部对白框 |
| L2 特殊动画 | 短动画、干预窗口、反馈和数值结算 | 底部对白框 + 动画叠层 |
| L3 终章 | D7 镜头演出、黑屏回响、结局页 | 底部对白框 |

### 5.1 普通对白

- 主角左侧、NPC 右侧，构图需要时可整体反转；
- 当前说话者缩放 1.08–1.15、亮度 100%；
- 非当前说话者缩放 0.82–0.92、亮度约 55%；
- 切换使用 150–250ms 动画；
- 底部台词框每次显示一句，点击、空格或触摸继续；
- 支持逐字、跳过当前句、自动播放、文字速度和已读快进；
- 没有现成 NPC 台词时不新增台词；
- 玩家悄悄话使用内容指定的 speaker 和金色字。

### 5.2 特殊动画

| 节点 | 表现 |
|---|---|
| D1 批评 | 黑雨云、巨手捂耳或金色拉链 |
| D2 成绩单 | 成绩单、同学聚拢、红叉、塞糖/悄悄话 |
| D4 家长来电 | 手机震动、抱抱或拍拍肩 |
| D6 考后冒雨 | 雨中操场、拨云见光 |
| D6 排名公布 | 黑雨云、排名、三种干预 |
| D7 终章 | 镜头推近、主角抬头、黑屏回响 |

动画期间隐藏移动和其他输入。跳过已看动画仍需执行完整结算；表现重播不得重复加分。

---

## 6. 内容数据契约

`src/types/content.ts` 是合同。正式内容已扩展成以 `Moment` 为核心的数据，并复用既有 `flow`、`dialogue`、`dilemma` 引擎能力。

### 6.1 顶层配置

```ts
interface GameConfig {
  meta: { title: string; version: string };
  assets: AssetManifest;
  maps: Record<string, MapConfig>;
  scenes: Record<string, SceneConfig>;
  moments?: Record<string, MomentConfig>;
  dilemmas: Record<string, DilemmaConfig>;
  flow: FlowNode[];
  endings: EndingConfig[];
  start: string;
  settings?: GameSettings;
}
```

`moments` 已接入。**没有**引入 `dialogues` 顶层字段：一个时刻的台词直接写在 `moment.lines` 里，
避免"时刻表"和"对白表"两份数据互相漂移。`dilemmas` 保留为引擎能力（示例与测试用），
正式七日内容的事件写在 `moment.event` 内。

### 6.2 MomentConfig（实际实现）

```ts
interface MomentConfig {
  id: string;                      // 同时作为流程节点 id（flow 里 id 就是 momentId）
  onceKey: string;                 // 防重复；写入 state.onceKeys
  day: number;                     // 1–7
  time: string;                    // HH:mm，用于排序与地点提示
  locationId: 'bedroom' | 'classroom' | 'playground';
  presentation: 'L0' | 'L1' | 'L2' | 'L3';
  title: string;                   // 地点提示与调试入口上显示的时刻名
  sceneId: string;
  lines?: DialogueLine[];          // 独白/对白
  outcome?: Outcome;               // 结算（心情等）
  event?: {                        // 有事件才有干预窗口
    signal: Signal;
    intro?: DialogueLine[];
    interactions: Interaction[];   // 合法的干预，一次只列当前事件允许的
    timeoutSec?: number;
    onTimeout?: Outcome;           // miss
  };
  when?: Condition;                // 条件不满足则整段跳过
  next: string;                    // 紧邻的下一个时刻 / 下一天的 day 节点 / ending-pick
}
```

与原计划的差异（以实现为准）：不再单独引入 `Route`、`priority`、`windowStart/windowEnd`、
`triggerType`、`allowedInterventionIds`、`dialogueId`、`cinematicId`、`onLate`。理由：

- 严格按天线性推进时，时刻顺序由 `flow` 数组本身决定，不需要 `priority` 抢事件；
- 干预范围由 `event.interactions` 直接定义，不需要再写一份 `allowedInterventionIds`；
- 到达窗口只对"人物移动"有意义；当前用调试入口点击目标，不存在早到/晚到，
  因此 `windowStart/windowEnd`、`triggerType`、`onLate` 留到后期移动阶段再评估；
- 表现分级用 `presentation: L0/L1/L2/L3` 直接表达，不再拆成 `dialogueId`/`cinematicId`。

### 6.3 地图（实际实现）

```ts
interface MapConfig {
  id: string;
  background?: AssetId;            // 三张地图占位资源，美工 A 只替换同一 AssetId
  baseWidth: number;               // 坐标基准 = 该地点背景图的交付尺寸（1280 × 720，从 art.spec.json 读）
  baseHeight: number;
  locations: MapLocation[];
}

interface MapLocation {
  id: string;
  label: string;
  hit: { x: number; y: number; w: number; h: number };
  target?: string;                 // 后期人物移动接回事件节点时使用
  marker?: AssetId;
  when?: Condition;
}
```

`baseWidth/baseHeight` 是美工 A 出图的坐标基准。当前阶段地图只用于"下一目标"调试入口
的背景与地点名；`hit` 为后期人物移动预留，不参与当前判定，也**不使用碰撞**。

### 6.4 DialogueLine（实际实现）

```ts
interface DialogueLine {
  id?: string;
  speaker?: 'student' | string;    // 缺省 student；非 student 触发 NPC 立绘并高亮对方
  text: string;
  cloud?: 'dark' | 'white' | 'gold';
  typingMs?: number;
  collectible?: boolean;
  group?: string;
  effect?: Effect[];
}
```

没有 `portraitState` / `emotion`：当前立绘按"当前说话者 / 非当前说话者"两态渲染
（active 放大变亮、inactive 变暗），这已覆盖文档的硬验收项。等美工 B 交付带表情分档的
立绘后，再按实际格式扩展这两个字段，避免先写一套美术无法交付的枚举。

`text` 只能来自 V1.2 内容源；引擎不补台词。


### 6.5 干预与结算

```ts
interface DilemmaConfig {
  id: string;
  name: string;
  sceneId: string;
  signal: Signal;
  intro: DialogueLine[];
  interactions: Interaction[];
  timeoutSec?: number;
  onTimeout?: Outcome;
  next: string;
}

interface Interaction {
  id: string;
  label: string;
  cost: number;
  availableWhen?: Condition;
  outcome: Outcome;
}

interface Outcome {
  moodDelta?: number;
  bondDelta?: number;
  lightDelta?: number;
  setFlags?: Record<string, string | number | boolean>;
  feedback?: DialogueLine[];
  effect?: Effect[];
  next?: string;
}
```

光不足时按钮置灰，不额外制造 miss。窗口结束且未选择时执行 `onTimeout`。结算只允许执行一次。

### 6.6 流程节点

```ts
type FlowNode =
  | { id: string; kind: 'map'; mapId: string; next?: string }
  | { id: string; kind: 'scene'; sceneId: string; next?: string }
  | { id: string; kind: 'dilemma'; dilemmaId: string }
  | { id: string; kind: 'dialogue'; lines: DialogueLine[]; outcome?: Outcome; next?: string }
  | { id: string; kind: 'choice'; choices: Choice[] }
  | { id: string; kind: 'set'; set: Record<string, Scalar>; next?: string }
  | { id: string; kind: 'branch'; branches: Branch[]; fallback?: string }
  | { id: string; kind: 'random'; branches: WeightedBranch[] }
  | { id: string; kind: 'day'; day: number; light: number; title?: string; next: string }
  | { id: string; kind: 'ending'; endingId: string };
```

`day` 负责 DAY_INIT、重置守护之光和记录每日心情；`random` 仅用于内容已有的随机结果；不可干预时刻使用 `dialogue.outcome`，不能显示选择按钮。

---

## 7. 运行时状态与数值

```ts
interface GameState {
  contentVersion: string;
  nodeId: string;
  day: number;
  storyTime: string;
  locationId: string;
  playerPosition: { x: number; y: number };
  currentMomentId?: string;
  dialogueIndex: number;
  onceKeys: string[];
  inputMode: 'mouse' | 'keyboard' | 'touch';
  flags: Record<string, string | number | boolean>;
  mood: number;
  bond: number;
  light: number;
  dayMoods: { day: number; mood: number }[];
  logs: {
    interventions: Record<string, number>;
    totalInterventions: number;
    missCount: number;
    collected: string[];
  };
}
```

规则：

- 心情初始 45，范围 0–100；
- 成功干预按内容增加羁绊；
- D1–D5 每日 2 点守护之光，D6 为 3 点，D7 关闭干预；
- 日末清零、不结转；
- 普通事件窗口 10 秒，第 6 天最后一夜 15 秒；
- 睡眠使用跨夜时长，`< 7.25 小时`触发睡眠不足；
- 结局条件和文本严格使用 V1.2 内容提供值，引擎不改阈值。

存档键为 `localStorage['jks2.save']`，保存当前日、时间、地点、当前 Moment、对白索引、onceKeys、数值和日志。人物 `position` 字段为后期移动增强预留，当前阶段不依赖它，也不实现碰撞恢复。内容版本变化时不得崩溃；不兼容进度可重置，但允许保留的收藏和留言应保留。

---

## 8. 引擎系统职责

### 8.1 Engine / FlowRunner

维护唯一状态并按节点推进；所有异步表现使用 Promise；对白、动画和事件期间锁定输入；节点完成后存档。

### 8.2 MapSystem

当前只渲染固定路线、目标箭头和地点调试入口；点击调试入口后检查 day、location、time、onceKey 和 priority，并播放过渡。地点按钮仅用于开发调试，不是正式玩法。后期人物移动加入时，MapSystem 再接收角色移动状态并根据剧情顺序触发地点，不增加碰撞系统。

### 8.3 SceneManager

组合背景、主角、NPC、前景与天气；执行约 1 秒转场和 `onEnter` 效果；按设计基准响应式缩放。

### 8.4 DialogueSystem

底部单句台词框；点击/空格/触摸推进；逐字、跳过、自动播放、已读快进；根据 speaker 控制立绘 active/inactive；收藏句只入图鉴一次。

### 8.5 ChoiceSystem

按钮出现时启动倒计时；校验光和条件；成功后锁输入并执行结果、动画和反馈；超时执行 miss；结算后立即清理旧按钮。

### 8.6 AnimationSystem

名称到实现的注册表，使用 CSS/Web Animations API。首发能力：台词淡出（原来的"云朵消散"，云朵容器删掉后作用在对白框上）、巨手、拉链、糖果、时钟、拨云、雨幕、手机震动和终章镜头推近。内容只写动画名，引擎注册实现。

### 8.7 AudioSystem

首次用户交互后初始化 Web Audio API；支持脚步、地点环境、逐字提示、成功/miss、雨声、手机震动、拉链、糖果、时钟、拨云和终章和弦；静音偏好写入本地。

### 8.8 UISystem

显示当前地点、下一目标、移动箭头、底部对白框、说话者状态、事件按钮、倒计时、守护之光和结局入口；适配移动端安全区。

---

## 9. 点击层级与可访问性

`.scene` 使用独立 stacking context。覆盖内容层的装饰元素必须 `pointer-events:none`：

```text
.content       0   地图热区和普通界面
moment-cg      1   剧情插画（不拦截）
signal-layer   2   不拦截
stage-bottom   4   整条带子就是"继续"的点击区（立绘行 / 对白框 / dock 都在它里面）
   cast-layer      立绘行（不拦截，点击穿透到带子）
   bar             底部对白框 / dock
countdown      6   不拦截
toast          9   不拦截
day-card       12  转场层
ending         20  结局交互层
```

**场景里没有人图层**：`.student-layer` 与 `components/student.ts` 的通用 SVG 占位小人已整个删除
（它不分性别、越缺图越会出现）。场上的人只可能是正式立绘行或剧情插画，两个都没有时就是一间干净的空教室。

新增全屏层必须补 CSS 点击审计。交互使用 `<button>`，disabled 必须是真实属性；不能只靠颜色表达状态；支持键盘和足够的触摸面积；尊重 `prefers-reduced-motion`。

### 9.1 翻页只有一条路（点击与键盘同一个出口）

```text
点击（整条 .stage-bottom，含立绘）──┐
                                  ├──→ Engine.pressAdvance() ──→ 当前阶段注册的那个 handler
键盘（空格 / 回车，window 上的翻页监听）─┘
```

各阶段注册的内容：打字中 = 跳过打字把整句显示完；等下一句 = 翻到下一句；
干预窗口 = 「这次，就让它过去」（立刻继续）。

这条链路只管**有下一步可翻**的阶段。另外两处"等玩家一个动作"的过场各有自己的键盘通道，
它们**不是**翻页，所以不注册 handler，而是各自在 window 上挂一个**一次性的**监听：

| 过场 | 键盘 | 为什么不能塞进翻页出口 |
|---|---|---|
| 地点门「下一目标 · 前往 X」 | 回车 / 空格 = 点这颗按钮（`UiSystem.keyActivate`） | 入口出现时没有"下一步"可翻（`advanceHandler` 是 `null`），它是"去哪"而不是"继续" |
| 人物自动移动转场 | 回车 / 空格 = 直接到终点（`WalkSystem`） | 同上，且整块转场本来就是点击区 |

两处都遵守同几条让路规矩：焦点在任何按钮上时**不抢**（浏览器默认行为就是"按下它"，
抢了就成了一次按键两件事）、输入框里不抢、`e.repeat` 不算。地点门**没有**干预窗口那种
0.7 秒冷静期 —— 它不花守护之光、不跳过任何内容，一路按着回车读下去的玩家顺手把门
和走路转场一起按掉，正是他想要的。

从前点击是各阶段自己 `el.onclick = …`、键盘是 `waitTap` 里另挂一份，于是"打字过程中点击能跳过、
空格不能"这种不一致必然出现 —— 玩家看到的结论是"空格键不翻页"。现在两件事都只有一处定义。

键盘的边界（翻页在 `Engine.onAdvanceKey` 一处、地点门在 `UiSystem.keyActivate` 一处）：

| 情况 | 行为 | 为什么 |
|---|---|---|
| 焦点在输入框里 | 空格照常打字，不翻页 | 结局页留言瓶要能输入空格 |
| 焦点在按钮上（干预按钮 / 地点门） | 空格 / 回车 = 按下这颗按钮（不抢） | 否则键盘用户没法用 Tab + 回车做选择 |
| `e.repeat`（按住不放） | 不算连翻 | 免得"按住空格读快点"一路冲过干预窗口 |
| 干预窗口刚打开的 0.7 秒 | 键盘不算数，**点击永远立刻生效** | 刚按完最后一句的手还在空格上，那不是"决定不帮忙" |
| 地点门还在场上时「重新开始」 | 旧监听自我注销（按钮一离开 DOM 就失效） | 否则下一轮在问卷上按回车会被它把 `contentEl` 清空 |

HUD 按钮（自动 / 速度 / 静音 / 重开）点完会 `blur()` 把焦点还回去 —— 否则焦点留在按钮上，
之后按空格就变成"再按一次这颗按钮"，玩家看到的同样是"空格不能翻页"。


---

## 10. 工程命令与打包

```bash
npm install
npm run dev       # 5180，避免本机其他项目占用 5173
npm run build     # tsc --noEmit + 单文件构建
npm run preview   # 127.0.0.1:4180，稳定预览 dist
npm run test
npm run test:watch
```

`dist/index.html` 必须零外部 JS/CSS 引用。开发环境的文件 watcher 与 Windows 原子写入存在锁冲突，稳定试玩优先使用 `build + preview`；preview 不监听源码。

**不要用 PowerShell 的文本 cmdlet 改源码文件**（`Get-Content -Raw` → `-replace` → `Set-Content`）。
它按**系统 ANSI 代码页**读写，含中文的 UTF-8 文件会被整份重编码：实测一次改动就把
`tests/browser/smoke.mjs` 里 192 行、409 处中文吃成 `?`（每个损坏点吃掉"某个汉字的末字节 + 紧邻的一个字节"，
连行尾换行也会被吃掉，于是注释把下一行代码吞掉、语法直接崩）。
改文件一律走编辑器 / 补丁工具；确实要脚本批改，就用 Node 的 `readFileSync(..., 'utf8')` / `writeFileSync`
（它按 UTF-8 读写，不经过任何代码页转换）。

当前已验证的稳定试玩地址：`http://127.0.0.1:4180/`（`npm run preview` 托管 `dist/`）。改完源码必须重新 `npm run build` 再刷新页面。

---

## 11. 测试验收

### 11.1 自动测试

- Condition 组合与数值比较；
- 心情夹紧、守护之光消耗；
- 睡眠跨夜与 7.25 小时阈值；
- onceKey 防重复；
- day 节点与每日资源；
- 存档与版本迁移；
- 图鉴去重；
- CSS 浮层点击审计；
- 标题页到结局页的整局点击链路（43 个时刻全走完）；
- 地点调试入口只给一个目标（画面上的调试标签已按需求删除，改由 DOM 断言守"标签不能回来"）；
- 光不足时按钮置灰和超时 miss；
- 七日严格顺序、逐日推进与不跳天；
- 刷新恢复天/地点/onceKey 且不复播已完成时刻；
- 台词语气色（脚本的云朵灰/白/金 → `.dialogue-box[data-tone]`）与收藏句 id 完整性。

### 11.1.1 真浏览器测试（Playwright）

`tests/browser/smoke.mjs`，通过 `npm run test:browser` 运行（需先 `npm run preview`）。
优先使用本机已安装的 Chrome/Edge，不依赖下载浏览器。它负责 jsdom 结构上做不到的事：

- 浮层是否真的挡住点击（`elementFromPoint` 实测命中元素）；
- 计算样式是否符合 §5.1 的**具体数值**（立绘 scale 与 brightness，从 `matrix()` / `filter` 里读，
  不拿 `opacity` 当"亮度"）；
- 占位图是否真的解码成功（`naturalWidth > 0`），而不是裂图或 0 高度；
- 对白能力真的生效：逐字速度 A/B 对照、已读快进、自动播放不点鼠标也推进
  （计时用页面内 `MutationObserver`，不用往返轮询，理由见 §13.4e）；
- `prefers-reduced-motion:reduce` 下装饰动效归零、**而倒计时条仍保留真实时长**；
- 零 404、零控制台错误、零外部资源请求；
- 390px / 768px 移动端不横向溢出；
- 同时产出 `tests/browser/shots/` 截图（当前 10 张）供人工目视。

当前规模：**46 条断言全通过**；jsdom 侧 `npm test` 为 13 个文件 / 111 项。

### 11.2 V1.2 硬验收

状态说明：`[x] 自动` = 已由自动化测试断言；`[~]` = 已实现但只能人工目视确认；`[ ]` = 未完成。

- [x] 自动　D1 → D7 严格按天、按顺序推进（`tests/unit/sevenDay.test.ts` 断言流程顺序与逐日出现）；
- [x] jsdom　三地点的调试入口可验证目标顺序和剧情触发（整局点击链路覆盖 43 个时刻）；
- [x] 真机　地点按钮仅作为调试入口，一次只给一个目标；整屏只有一个热区，按钮真的能点（`elementFromPoint` 未被浮层遮挡）；卡片上那枚「开发调试入口 · 非正式玩法」标签已按需求删除，并断言它不会被加回来；
- [x] jsdom + 真机　地点入口也能按回车 / 空格（等于点「前往 X」）；按住不放不算连发；焦点在按钮上时不抢按键（真机用真按键验，jsdom 里未受信任的事件不触发默认行为）；
- [x] jsdom　当前阶段不含人物移动与碰撞（内容与代码中均无此能力，测试断言不验收）；
- [x] 真机　普通对白逐句推进（L1 底部对白框走到结局）；
- [x] 真机　当前说话者放大变亮，另一方变暗（比对两侧立绘的计算 `transform` 与 `opacity`）；
- [x] jsdom　对白/动画期间锁定地点入口与剧情输入（`state.inputMode`）；
- [x] jsdom　事件窗口时间正确：普通事件 10 秒、D6 最后一夜 15 秒（内容断言 + 超时走 miss）；
- [x] jsdom　D2 体育课改自习无干预入口，只播台词并结算 −10；
- [x] jsdom　D7 无干预并进入终章回响，点题句为「感谢你，撑过来了。」；
- [x] jsdom　刷新恢复天、地点、已完成的 onceKey，且不复播已完成时刻；
- [x] 真机　结局页显示七朵云、守护星级、图鉴计数与留言瓶入口；
- [x] 真机　三张地图与两个立绘槽位的占位图真的画出来（`naturalWidth > 0`）；
- [x] 真机　单文件构建产物零 404、零控制台错误、零外部 JS/CSS 请求；
- [x] 真机　390px / 768px 宽不横向溢出；
- [x] 真机　空格 / 回车都能推进对白，且**打字过程中按一次就把整句显示完**（不只是鼠标可点）；
- [x] 真机　干预窗口里必有「这次，就让它过去」：不写"不帮助"、写明不消耗守护之光、点了立刻继续、点完立刻收掉；
- [x] 真机　17 个干预窗口每一个都有人物画面（立绘行或剧情插画），且画面上没有占位人物图形；
- [x] jsdom　点立绘 = 摸摸头（心情 +2、飘一句、每一幕限一次），且这一下照样翻页（不留死点击）；
- [x] jsdom　槽位解析对账：每个时刻的每个插画槽位、男女两条路线都解析得到**已交付**的图；
- [x] 真机　干预按钮副标题写明"消耗守护之光"，且全文没有"元"这类货币说法；
- [x] 真机　底部两条横栏（对白框 / 干预按钮）都不压住立绘（立绘行在文字带之上）；
- [x] 真机　日转场卡播完才出现"下一目标"入口，两者不叠在一起；
- [x] 真机　结局浮层出现时场景演出层已收干净（立绘不会从卡片下面露出来）；
- [x] 真机　推近镜头不会把左上角地点名推出场景边界切成一截乱码；
- [x] 目视　已按 `tests/browser/shots/` 截图逐张核对过版面（对白框、干预、调试入口、结局、移动端）；
- [ ] 　　　美术观感与动画手感的最终人工确认（等正式美术资源到位后再做一次）。

真浏览器验收命令：先 `npm run preview`，再 `npm run test:browser`，截图落在 `tests/browser/shots/`。
截图与断言是互补的：断言只能保证"尺寸、层级、点击命中、计算样式、数值"这些可量化的部分，
观感必须人看截图。真浏览器测试已经先后抓到 4 个 jsdom 结构上抓不到的 bug，务必保留这一层：

1. 地点过渡的 1 秒里，已点过的"前往"按钮仍留在 DOM 里且可命中，会吃掉玩家接下来的点击
   （表现就是"点了没反应"）。现在入口一被点击就立刻从 DOM 移除。
2. 对白框打字过程中，点在框内空白处没反应（跳过句子的处理器只挂在文字节点上）。
3. Chrome 探测 `/favicon.ico` 造成 404（单文件构建不该有任何外部请求）。已改为内联 data-URI 图标。
4. 坐姿（`data-pose=sit`）的 `margin-bottom` 覆盖了小人的让位距离，导致干预按钮压在小人身上。
   （那一版已随占位小人一起删除：场景里不再有人图层，`padding` 与体态的互相覆盖也就不存在了。）
5. **D2 08:00「成绩单来了」的整个干预窗口没有人物图**：围观那张是 `who:'common'` 的图，
   却被写成按性别的 `S('crowd')` → 拼出 `art.story.boy.crowd` 这个不存在的 AssetId →
   插画层静默清空，画面上只剩一间空教室 + 那个丑占位小人。编译、单测、真浏览器验收全绿，
   因为"少一张图"和"这一刻本来就没图"长得一模一样。修法有三层：
   `S()`/`C()` 各守一边（用错入口加载期抛错）、槽位解析逐条对账（`tests/unit/storyArt.test.ts`）、
   真浏览器逐窗口取样"这一屏必须有人"（`tests/browser/smoke.mjs`）。
6. 打字过程中按空格毫无反应（只有点击能跳过打字），玩家据此得出"空格键不翻页"的结论；
   干预窗口里空格同样没反应，于是每一次倒计时都被干等完。已改为点击与键盘共用
   `Engine.pressAdvance()` 一个出口（见 §9.1）。

---

## 12. 两天排期

### 第一天：D1 垂直切片

- 三地点、固定路线、目标调试入口和目标提示；
- 点击调试入口后的过渡、剧情触发和 onceKey；
- 普通对白、立绘 active/inactive；
- D1 特殊动画、干预、反馈和 miss；
- 三张占位地图、占位主角立绘、对白框和一个 VFX；
- 人物移动控制、行走贴图和碰撞不在本阶段实现。

验收路线：

```text
卧室 → 教室 → 普通/特殊剧情 → 操场 → 剧情 → 教室 → 卧室
```

### 第二天：D2–D7 与打包

- 复用 D1 机制接入其余六日，严格按天推进；
- 接入心情、羁绊、每日守护之光、睡眠和结局；
- 存档、刷新恢复、地点按钮点击；
- 关键节点 D1、D4、D6、D7 动画；
- 脚步、环境和关键音效；
- 用占位资源完成全流程，正式图片后续只做资源替换；
- 全量测试、人工试玩和单 HTML 打包；
- 人物移动控制列为后期独立阶段，不阻塞本阶段 Demo。

当天出口：七日可从头通关，其他非关键节点使用普通对白或环境独白。

---

## 13. 当前项目状态与下一步

| 项目 | 状态 |
|---|---|
| TypeScript + DOM/CSS/SVG + Web Audio API | 已采用 |
| 游戏引擎 | 未使用，不应引入 |
| Vite 单文件构建 | 已接入，`dist/index.html` 零外部引用 |
| 三张场景地图占位图 + 稳定 AssetId | 已接入（占位 SVG，美工 A/B 只替换 `src`） |
| 地点调试入口（一次只给一个目标；标签已删） | 已接入 |
| D1→D7 严格按天推进（44 个时刻定义，一周实际走 43 个） | 已接入，有严格顺序测试 |
| L0 环境独白 / L1 底部对白框 + 立绘高亮 / L2 事件 / L3 终章 | 已接入 |
| 干预窗口、miss、每日守护之光、心情、羁绊 | 已接入 |
| 音效（文档 §8.7 的 12 种，Web Audio 实时合成、无音频文件） | 已接入（详见 §13.4） |
| 对白能力：逐字 / 跳过当前句 / 自动播放 / 文字速度 / 已读快进 | 已接入（详见 §13.4） |
| iPhone 安全区（`env(safe-area-inset-*)`）与 `prefers-reduced-motion` | 已接入（详见 §13.4） |
| 存档、刷新恢复、七日云朵、结局页、图鉴、留言瓶 | 已接入 |
| 人物移动控制 | **后期再加，不属于当前核心系统** |
| 碰撞、物理、寻路 | **不实现** |
| 普通 NPC 台词内容 | 源脚本未提供，立绘槽位已预留 |
| 正式美术和音频文件 | D1–D7 的美术**已全部接入**（`art/` 80/85 张，详见 §14.3）；音频仍为实时合成 |
| Playwright 真浏览器验收 | 已接入（`npm run test:browser`），断言全通过并产出 10 张截图；另有逐帧画廊 `npm run shots:day1 / day23 / day47` |
| 键盘推进对白（空格 / 回车） | 已接入 |
| 键盘过场（地点门「前往 X」、人物自动移动转场都能按回车） | 已接入（详见 §9.1） |
| 心情数值配平与结局可达性 | 已按实测重标（详见 §13.2），四个结局都可达 |
| 睡眠规则（睡眠 < 7.25h 才触发睡眠不足） | 已接入（详见 §13.3b），作息定制真的会改变这一周 |
| D5 羁绊检查点（羁绊 ≥6 走金色版） | 已接入（详见 §13.3c） |
| 美术交付管线（`art/` → 内联单文件） | 已接入并完整演练过（详见 §14） |
| 金钱 / 货币设定 | **不存在，也不应有**（详见 §13.1.1；唯一资源是"守护之光"） |

当前阶段尚未完成的部分只有两项：**正式美术/音频替换**与**美术观感的最终人工确认**。
人物移动控制仍然按文档要求放在后期，届时只替换"目标地点选择"这一层，不改变 D1 → D7 状态机，
也不引入碰撞系统。
已经做完的关键改造：

1. `content/moments.ts`：44 个时刻的结构化内容（D5 羁绊检查点按羁绊走两个互斥分支）（天、时间、地点、表现级别、台词、事件、结算、下一目标）；
2. `content/flow.ts`：`day-N → 当天时刻 → day-(N+1)` 的严格线性编排，引擎不存在跳天入口；
3. `src/core/engine.ts`：`moment` 流程节点，按 `onceKey` 防重复，切换地点时先给"下一目标"调试入口再播放约 1 秒过渡；
4. `src/systems/dialogueSystem.ts`：**底部对白框是唯一的语言通道**（云朵容器已删除，见 §5 的变更说明），含立绘 active/inactive 高亮；
5. `src/core/runtime.ts`：`storyTime`、`locationId`、`currentMomentId`、`onceKeys`、`inputMode` 状态与存档字段；
6. `content/assets.ts`：三张地图、主角/NPC 立绘、雨幕、结局卡的稳定 AssetId 与占位资源。

---

## 13.1 待编剧/策划确认的问题（不得由开发擅自定稿）

以下问题在源脚本里自相矛盾或没有定义。开发按"选一个并写进测试"的方式先推进，
**每一项都需要编剧/策划点头或改口**，改口后只动内容层，不动引擎。

1. **D7 终章点题句有冲突。** 脚本正文（第 303 行）写「谢谢你守护我。」，
   而脚本自己的改稿记录（第 368 行）明确写"改为「感谢你，撑过来了。」（终稿点题句）"。
   开发按**改稿记录**取「感谢你，撑过来了。」，并在测试里断言终章**不得**再出现「谢谢你守护我。」。
   请确认以哪句为准；若以正文为准，把测试里那条反向断言一起改掉。

2. **"平均心情"没有定义口径。** 脚本结局表只写"平均心情 ≥66 / ≥42"，没说是
   ①所有心情记录点的平均，还是 ②每天锚点的平均。两种口径结果差别很大（事件多的日子权重完全不同）。
   开发两个都实现了：`avgMood`（记录点平均，**当前结局判定用的是这个**）与 `avgDayMood`（每日锚点平均）。
   请定一个。（另外 66 这个数本身不可达，见 §13.2 第 2 条，已暂标定为 62。）

3. **终章"低羁绊简化版"没有触发条件。** 脚本 D7 写"（低羁绊周目此处简化为……）"，
   但没给羁绊阈值。当前实现没有做这个分支。

4. **D2 体育课改自习只有"无干预"，没有说明后续。** 脚本要求这里不可干预、
   心情 −10，但没有写这句之后是否要补一句安慰台词。当前实现只播这一句就结算。

5. **契约里 `randomOutcomes`（干预随机成败）没有内容。** 脚本的干预文案只写了成功效果，
   没有写失败分支。当前内容全部按"必成功"处理，随机成败字段留着但没有内容使用它。
   如果策划要"干预有概率失败"，需要补失败文案与数值，否则会出现在 UI 上说不出话的情况。

6. **脚本的《目标心情》表与它自己的干预数值互相矛盾（已实测确认）。**
   脚本给的成功干预均值约 +10、miss 均值约 −8，而它给的守护之光又允许一周成功干预 12 次、
   强制 miss 只有 5 次 → 净变化 +80；但《目标心情》表（D1…D7 = 45/40/55/35/50/28/70）隐含的净变化只有 +25。
   无论怎么等比缩放都变不成 +25（缩放只会让整条曲线一起靠近起点 45）。
   当前按用户决定先做了一次 0.5 缩放把峰值从 98 压到 91（见 §13.2），
   但要真正对上 70，需要**编剧改一处**：要么把成功干预降到 +4 上下，要么把守护之光减到每天 1 点，
   要么承认 70 只是"没有你干预时"的基线、把《目标心情》表改成含干预的曲线。请选一个。

### 13.1.1 关于"金钱"的确认

**本作没有任何金钱 / 货币 / 购买 / 付费设定，也不应该有。** 全仓（除 node_modules/dist）搜索
`元|货币|金币|金钱|零花钱|价格|买单|花费|付费` 的结果里，`元` 只出现在"元素""单元测试"这类词里，
没有任何一处是货币含义。

玩家在干预按钮上看到的是**守护之光**的消耗：脚本定义每天给 2 点（D6 给 3 点，D7 不给），
每次干预消耗 1 点 —— 这是本作唯一的资源，来源是"每天醒来就有的守护"，不可购买。
早期按钮副标题只写了「消耗 1 光」，容易被误读成"1 元"，现已改为「消耗守护之光 ✦1」，
并且真浏览器测试里加了两条断言：副标题必须包含"守护之光"、且不得出现"元"这类货币说法。

### 13.2 数值配平的实测结果（已按用户决定调整）

`tests/unit/balance.test.ts` 会按 day 的守护之光、事件数与引擎记账方式跑多种玩法策略并打印实测值。
内容规模：44 个时刻定义（一周实际走 43 个）/ 17 个可干预窗口 / 全周共 13 点守护之光（D1–D5 各 2、D6 给 3、D7 不给）。

**重要说明（修正过一次）**：本项目早期的配平结论"选最暖与选最冷结果完全一样"是**测试自身的 bug**
造成的 —— 模拟脚本把心情读成了 `interaction.moodDelta`，而契约里它在 `interaction.outcome.moodDelta`，
两边都取到 `undefined`，于是两种策略都被当成"永远选第一个"。修正后重测，选择确实会影响结果
（最暖 65.7 vs 最冷 60.8），但**原始脚本数值下确实存在真实的饱和问题**（见下）。

修正后的实测（MOOD_SCALE = 0.5，结局阈值按实测重标）：

```text
全选最暖：              结局=晴空        平均心情=65.7  羁绊=12  干预=12  错过=5   周终心情=94
全选最冷：              结局=生长        平均心情=60.8  羁绊=12  干预=12  错过=5   周终心情=83
永远选第一个：           结局=晴空        平均心情=65.7  羁绊=12  干预=12  错过=5   周终心情=94
轮换·每人≤2 次：         结局=晴空        平均心情=65.5  羁绊=12  干预=12  错过=5   周终心情=93
只护睡眠（隐藏路线）：     结局=笨拙的守护   平均心情=28.8  羁绊=6   干预=6   错过=11  周终心情=24
只救 D1–D2：            结局=雨过        平均心情=37.6  羁绊=4   干预=4   错过=13  周终心情=12
只救 D1–D4：            结局=生长        平均心情=59.7  羁绊=8   干预=8   错过=9   周终心情=57
```

四个结局都可达（已断言），且形成"尽力→晴空 / 偏冷或只救前段→生长 / 基本不管→雨过 /
只用一种方式护着→笨拙的守护"的阶梯。三处改动与理由：

1. **心情数值统一 ×0.5（`content/moments.ts` 的 `MOOD_SCALE`）。**
   原始值下任何"能干预就干预"的玩法周终心情都顶到 98，而脚本《目标心情》表峰值只有 70。
   缩放保持脚本设计的相对强弱与正负号不变，把峰值压到 91。想恢复原值只需把 `MOOD_SCALE` 改成 1。

2. **晴空阈值 平均心情 ≥66 → ≥62（`content/conditions.ts`）。**
   66 在实测里**不可达**：脚本自己的曲线是 45 起步、D6 掉到 28、末尾回到 94，
   即使每步都选最优，50 个记录点的平均也只有 65.7 —— 脚本的阈值高于它自己的曲线能给出的上限。
   改成 62 后，"七天全程尽力且羁绊满 12"才够得着，摸鱼会落到生长/雨过。

3. **隐藏结局阈值 单一干预 ≥5 → ≥6（`content/conditions.ts`）。**
   实测一周里单一干预的理论上限是 `extraSleep=6`、`coverEars=5`，其余 ≤4。
   阈值 5 会跟"最优解"撞车（coverEars 在它出现的 5 个事件里都是收益最高的选项，认真玩就会点满 5 次），
   于是隐藏结局变成默认结局、盖住晴空。改成 6 之后，只有"每天都把光留给夜里那次偷加两小时"的玩家
   才拿得到 —— 代价是白天大量放手（实测平均心情只有 28.8、错过 11 次），
   正好对上「你总是用同一种方式，笨笨地护着我。」这层意思。
   **不能调到 7 以上**：那会超过内容上限 6，变成永远拿不到的死结局；
   `tests/unit/interventionCeiling.test.ts` 同时守着上下两端（≤ 上限、> 最优解上限）。

以上都只涉及 `content/` 的数值与 `settings`，改完重跑 `npm run test` 与 `npm run test:browser` 即可。

### 13.3 本轮补上的两条脚本规则，以及一个差点漏掉的 bug

**a) 结局阈值"写在两个地方"导致实际行为与配平结论不一致（已修）**

`endings.ts` 里 clumsy 的阈值改成了 6，但 `flow.ts` 的 `ending-pick` 分支里还写死着
`anyInterventionAtLeast(5)`。`ending-pick` 才是真正决定"走哪个结局节点"的地方，
所以**实际游戏在 5 次就判隐藏结局**，而配平测试按 6 算 —— 两边结论不一致，却都是"绿"的。
症状是：配平测试说"最暖玩法→晴空"，真浏览器跑出来却是"笨拙的守护"。

已统一为 `hiddenEndingCondition` 常量，并加了 `tests/unit/consistency.test.ts` 专门对账：
**凡是同一条规则写在两个文件里的地方，都要有一条断言证明两边说的是同一件事。**
修好后真浏览器与配平测试的结论一致（都给出晴空）。

**b) 睡眠规则没有接（已补）**

脚本第 48 行写「睡眠时长 < 7.25 小时即每晚触发「睡眠不足」事件」，但 6 个「睡眠不足」事件
之前是**无条件触发**的 —— 开局定制里的"闹钟""熄灯"两问**填什么都一样**。
现在按脚本补了门控（`content/rules.ts` 的 `sleepShort`），实测睡够 7.25 小时时一周少发生 6 个事件。

注意 `content/rules.ts` 与 `content/conditions.ts` 的分工：**基础条件放 rules，派生自内容的条件放 conditions**。
原因是 `conditions.ts` 需要 import `moments`（自动收集干预 id），如果基础条件也写在 conditions 里，
`moments.ts` 再去 import 它就会形成循环依赖，ESM 下 conditions 顶层的 `Object.values(moments)`
会在 moments 尚未初始化时执行并崩溃。

**c) D5 羁绊检查点只做了一半（已补）**

脚本第 236–239 行给了羁绊 ≥6 与 <6 两个版本，实现里只有"<6"那一版且无条件触发，
等于整条羁绊线在这一刻失效、金色句永远拿不到。现已按脚本补成两个互斥分支。

**d) 内容规模随之变化**：44 个时刻定义（D5 羁绊检查点是两个互斥分支，一周实际走 43 个）。
新增事件不影响可干预窗口数（分支两句都不可干预），17 个窗口与 13 点光不变。

### 13.4 表现层补齐：音效、对白四能力、可访问性（本轮）

**a) 音效按 §8.7 补齐 12 种，并去掉"静默降级"**

`AudioSystem` 用 Web Audio 实时合成，没有音频文件（零版权风险，也不给单文件构建增重）。
文档点名的 12 种与代码里的 `REQUIRED_SOUNDS` 一一对应，**单测核对覆盖**。

同时修掉一处很隐蔽的坏味道：旧版 `play(id)` 对任何不认识的 id 都回落成 `ding()`。
后果是"手机震动""拉链""糖果"全都会发同一个"叮"——不报错、只是听起来不对，极难发现。
现在未知 id 只 `console.warn` + 不出声，并有单测钉住这个行为。

另外补了两处**内容层没写、但文档要求有**的声音：miss 之前完全没声音；D4「爸妈来电」的
手机震动没有接（脚本 §5.2 要求）。内容只写动画名时，由 `effectRunner.ts` 的 `ANIM_SOUND`
按动画名配音；**刻意不出声**的 5 个动画（捂耳朵 / 暖光 / 云散 / 震动 / 推近）也登记在表里，
给动画改名时会 warn 而不是悄悄变哑。

**b) 对白四能力（§5.1）**

| 能力 | 实现 |
|---|---|
| 逐字 | `typingMsFor()`，基准 38ms/字，内容可用 `line.typingMs` 覆盖 |
| 跳过当前句 | 打字过程中点一下，整句立刻显示 |
| 自动播放 | HUD「▶ 自动」，打完字停留 `1200ms × 速度倍率` 自动翻页 |
| 文字速度 | HUD 三档：慢 ×1.7 / 中 ×1.0 / 快 ×0.45，存 `localStorage` |
| 已读快进 | 读过的句子整句直接显示；已读键是 `来源#行号`，不依赖台词有没有 id |

这里踩到并修掉一个**会让功能完全失效**的 bug：`restart()` 保留了 `collected`，
但 `startFresh()` 紧接着又 `createState()` 把状态整个重建，**把 `seenLines` 冲掉了**。
而"已读快进"唯一的用处就是重玩时不再逐字重打 —— 清空等于这个功能永远不生效。
现在跨周目保留的数据（图鉴 / 通关次数 / 已读台词）**只在 `startFresh()` 一处维护**，
不再让调用方各自留一份。

真浏览器实测（页面内计时，见下）：

```text
慢档首读 732ms（理论 11×65=715ms） / 快档 196ms（理论 11×17=187ms） / 已读重播 0ms
```

**c) 可访问性与适配（§9）**

- HUD 按钮统一 `min-height`（桌面 32px / 移动 44px），之前静音与重开是纯文字、点起来很窄；
- iPhone 安全区：`viewport-fit=cover` + `.game` / `.stage-bottom`（立绘行 / 台词 / 选项都住在它里面）的 `env(safe-area-inset-*)`；
- `prefers-reduced-motion:reduce` 关掉装饰动效。

> **倒计时条必须排除在减动效之外**。它是"还剩几秒"的唯一视觉信息，关掉等于把信息也关掉。
> 它的时长是 JS 写在 `style="animation-duration:10s"` 上的**内联样式**，而减动效规则里的
> `!important` 会盖过内联样式 —— 所以只能从选择器层面用 `:not(.countdown i)` 排除，
> 不能"先全关、再给它写回来"（写回来会跟内联样式打架，也取不回原值）。真浏览器有断言守着。

**d) 立绘两态严格按 §5.1 的数值，并且改成按数值验收**

`.portrait.active` 用 `scale(1.12)` + `brightness(1)`，`.portrait.inactive` 用
`scale(0.87)` + `brightness(0.55)`，切换 200ms。

之前的实现是 active `brightness(1.1)`、inactive 只加一层 `opacity:.42` —— 有效亮度算下来约 36%，
既低于文档要求的 55%，语义上也把"变暗"和"淡出"混成了一件事。**而当时的测试只断言"谁比谁大"
这种相对关系，所以偏了也照样全绿。** 现在 CSS 单测与真浏览器断言都改成核对 §5.1 的**具体数值区间**，
并要求 `inactive` 不许再用 `opacity` 表达变暗。

**e) 测试写法上的一个教训：别用 Playwright 往返来计时**

量逐字速度时，第一版是在 Node 侧轮询"文字不再变化"，量出"慢 302ms / 快 2ms"——
**那量的是探测延迟，不是打字速度**（快档那个 2ms 其实是"第一次取值时句子已经打完了"），
断言当时还碰巧通过了。现在改成在页面内挂 `MutationObserver` 记录每次变长的时间戳，
数值才可信（见上面 b 的实测）。要计量，就在被测环境里计时，不要让观测手段的延迟进入测量结果。

---

## 14. 三方交付协议

| 角色 | 交付 |
|---|---|
| B | 时刻表、路线、目标、事件、干预、成功/miss、数值、结局条件 |
| 美工 A：环境与移动 | 卧室、教室、操场地图；小人行走贴图；后期使用的入口/出口与碰撞区域素材；天气和场景叠加效果。当前阶段只交付/替换环境占位资源，不实现移动和碰撞 |
| 美工 B：立绘与演出 | 主角/NPC 立绘；对白框；当前说话者高亮；D1 重点动画；结局卡模板 |
| A：游戏框架 | 引擎、数据合同、剧情顺序、地点调试入口、存档、测试、打包和本文档 |

**在用的 AssetId（只替换图片文件即可，不要改 id）**

整屏舞台图共用 **1280 × 720（16:9）** 一个基准；立绘 320 × 420、D1 剧情插画 480 × 640
是另一套用途（走 `object-fit: contain`，不进 cover 舞台）。

```text
map.bedroom.background     卧室基底（调试入口底图 + 无时相时的卧室）        1280 × 720
map.bedroom.morning        卧室·清晨（D1 06:20 黎明）                    1280 × 720
map.bedroom.night          卧室·夜灯（睡前）                             1280 × 720
map.bedroom.lightsOut      卧室·熄灯（睡眠不足）                          1280 × 720
map.classroom.background   教室基底（叠加层之下的那一张）                   1280 × 720
map.playground.background  操场背景                                    1280 × 720
overlay.classroom.morning  教室·清晨光（要透明通道）                       1280 × 720
overlay.classroom.night    教室·夜间光（要透明通道）                       1280 × 720
overlay.rain               雨幕叠加层（要透明通道）                        1280 × 720
portrait.player            主角立绘（L1 底部对白框左侧槽位，未定性别兜底）     320 × 420
portrait.player.boy/girl   按性别的主角立绘（运行时只加载对应性别那一份）      320 × 420
portrait.player.*.rest     主角·放松态（最近一次干预成功后）                320 × 420
portrait.teacher           老师·普通站立（男女共用）                       320 × 420
portrait.teacher.strict    老师·严肃批评（npcPose='strict'）             320 × 420
portrait.npc               NPC 立绘（对白框右侧槽位，当前无台词）           320 × 420
art.customize.boy/girl     开局角色选择的基础形象                         400 × 533
art.d1.<boy|girl>.<pose>   D1 剧情插画（pose 见 §14.2）                  480 × 640
ending.card                结局场景背景（晴空 / 雨过共用）                 1280 × 720
```

### 14.1 时相场景（同一个房间的不同时段）

同一天里 06:20 的卧室和 23:20 的卧室不该是同一张画面，早读的教室和晚自习的教室也不该。
实现方式是**场景变体**而不是给时刻加字段：引擎本来就有 `moment.sceneId` 这条路
（`locationId` 管地点与地图，`sceneId` 管画面），时相落在场景上不需要新增引擎概念。

```text
地点场景（其它天共用）   classroom / bedroom / playground（带雨幕）
D1 时相变体             bedroomMorning / bedroomNight / bedroomLightsOut
                       classroomMorning / classroomNight / playgroundRunning
D2–D7 按天交来的空镜     playgroundWindy（D3）
                       classroomFoggy / classroomDusk（D4）
                       classroomEarlyLight / playgroundGentle（D5）
                       classroomMakeup / playgroundRainRun / playgroundClearing（D6）
                       playgroundBlueSky / classroomPenSound / classroomFinale（D7）
记忆回响                memoryEcho（终章推近之后、结局台词之前的那段黑屏）
```

- **卧室**用三张独立底图（清晨 / 夜灯 / 熄灯）：光线差别大到换底图更划算。
- **教室**在 D1 用一张底图 + 一层光（`overlay.classroom.morning` / `.night`）：
  底图只有一张，教室的陈设才不会因为换了时段就对不上，美工要维护的底图也只有一张。
  **D2 起按天交来的教室空镜是各自独立的整屏画面**（构图本来就不一样，比如 D4 的"黄昏粉笔灰"
  是橘色夕阳），所以直接换底图、不再叠光 —— 一份交付一天，一天里换时段就是换场景。
- **操场**分成 `playgroundRunning`（不带雨幕、`theme` 也不是 `rain`，否则会拉起雨声）
  与基底 `playground`（带雨幕）—— 脚本里只有 D6 15:00「考后冒雨」是在操场上明确下雨的。
  D5/D6/D7 又各交了不同天气与光线的操场空镜（温柔的风 / 考后冒雨 / 雨后放晴 / 天好蓝）。
  `playgroundRainRun` 的雨**画在底图里**，所以它不挂 `overlay.rain`；`theme` 仍是 `rain`
  （它拉起的是雨声环境音，而这一刻确实在下雨）。

**D1–D7 的 43 个时刻现在全部接上了各自的场景**，没有"某天还在走基底"的遗留。
接线方式是在 `content/moments.ts` 里给那一刻套一个 `at('<sceneId>', …)`，不需要改引擎。

### 14.1.1 干预结果换整屏天气（`MomentArt.successScene` / `missScene`）

D6 15:00「考后冒雨」的交付是**两张整屏天气图**，说明书写明"根据干预结果切换"。
原有的 `art.success` / `art.miss` 只能换**插画卡**，换不了整屏的天气（背景照样在下雨），
所以 `MomentArt` 多了两个场景槽位：

```ts
art: { successScene: 'playgroundClearing' }   // 干预成功 → 换成放晴那张底图
```

- 顺序是**先换场景、再换插画**：反过来的话新插画会在旧天气上闪一下。
- 场景 id 写错会**当场抛错**（`未知场景：…`）。这类错在运行时的表现是"画面没换"，
  而画面没换不像 bug（玩家只会以为这一刻本来就这样），所以不能让它安静地过去。
- `dataset.scene` 由 `SceneManager.show()` 统一更新（不再由各调用点自己写）——
  结局与记忆回响那条路也要换场景，漏标一次，真浏览器验收就会拿着上一幕的 sceneId 对账。

### 14.2 美术交付管线（已实现并完整演练过）

正式图**不再需要改 `src`**，流程是：

1. 美工把图按 **AssetId 命名**放进 `art/`，例如 `art/map.classroom.background.png`；
2. `node scripts/art.mjs check` 校验文件名与尺寸 —— 不对就**直接让构建失败**并说明错在哪；
   `npm run build` 与 `npm test` 都会自动先跑这一步（`prebuild` / `pretest`）；
3. `content/assets.ts` 用 `import.meta.glob` 自动收 `art/` 下的图，
   Vite 按 `assetsInlineLimit` 把它们**内联成 data URL**，所以单文件产物依旧零外部请求；
4. **没交的 AssetId 自动回退到内置占位 SVG**（并在 manifest 上打 `placeholder: true`），
   所以可以一张一张替换，中途永远可跑可测。

> **回退链必须问 `assets.isPlaceholder(id)`，不要去嗅 `src` 前缀。**
> 交付格式里允许 `.svg`，真交付的 SVG 同样是 `data:image/svg...`，
> 靠前缀判断会把真图误判成占位图，于是"没交放松态就退到正常立绘"这类回退会静默失效。
> 同类的第二个坑：格式（`kind`）只能从**交付文件的扩展名**推，不能从 `src` 的 data URL 前缀推 ——
> Vite 构建时会内联成 data URL，而 Vitest 里同样的 `import.meta.glob` 拿到的是 `/art/xxx.jpg` 路径，
> 前缀法在测试环境里会一律回退成 `png`，变成"只有测试环境错、构建环境对"这种最难查的错。

规格的**唯一真相来源**是 `content/art.spec.json`（尺寸、说明、体积上限），
`scripts/art-lib.mjs` 读它做校验，`content/assets.ts` 从它读尺寸基准，
`content/map.ts` 的 `baseWidth` / `baseHeight` **也直接读它** ——
避免"清单里写一个尺寸、代码按另一个尺寸裁"。这条以前只靠纪律，现在靠代码：
`tests/unit/sceneArt.test.ts` 会逐个地图对账，`tests/unit/art.test.ts` 会要求
所有整屏舞台图（地点背景 + 叠加层 + 结局背景）尺寸完全统一。

> 时相场景引入后，环境交付是**按原生出图尺寸**收的（1672×941 ≈ 16:9），
> 由 `scripts/import-scene-art.ps1` 统一重采样到规格尺寸。脚本会把每张图的横纵缩放偏差
> 打出来（这批是 0.053%，亚像素级）；偏差大就说明"要么裁、要么拉伸"，该回头和美术确认比例，
> 而不是硬压过去。这个脚本刻意写成纯 ASCII —— Windows PowerShell 5.1 按系统 ANSI 代码页
> 读 `.ps1`，含中文的 UTF-8 无 BOM 脚本会被解码坏掉，报出来的错与真实原因完全无关
> （踩过一次：`Bitmap::FromFile` 报"路径的形式不合法"，真实原因是中文注释把 `param` 块吃掉了）。

立绘槽位按 320×420（约 3:4）留位并用 `object-fit: contain` 渲染，
**正式立绘是整个人，不会被裁成方块**；槽位宽高比必须与 `art.spec.json` 一致。

演练方式与结论（已实际执行）：`node scripts/art.mjs drill` 生成一套尺寸合规的假图，
构建体积从 69 KB 涨到 140 KB、7 张图全部内联为 base64、真浏览器里确实画出了条纹、
零 404、单元测试与真浏览器断言全绿；删掉假图后又正确回退到占位图。

> 演练还顺带修掉一个隐患：真浏览器断言原本写死了"背景必须是 `data:image/svg`"，
> **美工一交图这条断言就会误报失败**。现已改成与格式无关（`data:image/` + 长度），
> 并且由 Node 侧读 `art/` 目录、和页面实际用到的图**对账**：
> 交了正式图就必须看到正式图，没交就应当看到占位图。

交付清单给美工看的版本在 `美术交付清单.md`（含文件名、尺寸、注意事项、截图对照）。

### 14.3 D4–D7 素材接入（本轮）

**结果**：D4–D7 四个交付包全部接入，**新导入 35 张图**，D4–D7 的 **22 个时刻全部挂上画面**。
`npm run art:check` 从 45/46 涨到 **80/85 张已交付**（剩 5 张：`portrait.player` / `portrait.npc`
两张已声明不再需要，`art.story.<who>.lookUp` 与 `art.story.common.rankProjection` 两张是
**预留 AssetId** —— 交付包里确实没有或不能用，详见《待确认问题》6.9）。

**几个值得记住的判断**：

1. **同一张交付图可以同时是"插画"和"道具"**：D4 20:00 用 4:3 的人物插画 + 1:1 的手机特写
   两张并排（引擎原有的"2 张并排"布局），`cg` / `during` 都不需要新增概念。
   道具按 **512×512** 收 —— 两张并排时卡片高度是舞台的 88%，512 在这个尺寸上就是 1:1。
2. **4:3 的"空镜"要当插画卡、不能当底图**：D6 12:00 交来的 `教室_饭有点凉` 是 1216×912，
   而别的教室空镜都是 1368×768。整屏底图是 `background-size:cover`，4:3 铺上去会**裁掉 25% 的画面高度**，
   所以那张按 640×480 的插画卡接（导入脚本对偏差 >0.5% 的目标尺寸直接报错，不会静默拉伸）。
3. **天气切换需要"换场景"而不是"换插画"**：见 §14.1.1 的 `MomentArt.successScene`。
4. **结局背景其实一直是空的**：`runEnding` 用的是结局**自带**的 `SceneConfig`，
   而不是 `config.scenes` 里那份同名场景，所以过去只写 `id` 的结局场景没有底图
   （D7 的「结局页_七朵云背景」正好把这个坑顶出来）。现在 `content/endings.ts` 里
   用一个 `endingScene()` 助手统一带上 `background`，并有单测对账"自带的那份"与"登记的那份"。
5. **记忆回响黑屏**：`memoryEchoLines()` 过去是和结局台词一次播完的，这轮拆成两段 ——
   先切到 `memoryEcho` 场景（D7 交的黑屏背景）播回响，再回结局场景播结局台词与 `zoomIn`。

**体积**：35 张新图把 `dist/index.html` 从 **4.7 MB 推到 6.8 MB**（单文件内联 base64，×1.33）。
转换后总体积 1.82 MB（原始交付 9.9 MB）。**这是当前最该盯的数字**：
再往里塞图之前，要先决定"继续内联 / 降 WebP 质量 / 拆成多文件"。

**验收物**：`npm run shots:day47` 产出 `tests/browser/shots/day47/index.html`（69 张，
男女各走一遍全程），每张都标着"这一刻该看到什么"。截图脚本本身也合并成了一份
（`tests/browser/day-shots.mjs`，支持 `--from/--to`），`day23-shots.mjs` 保留为一行参数的入口。

**美术资源替换原则：** 当前开发阶段全部允许使用任意占位图片或 CSS/SVG 占位表现。正式资源到位后，美工 A/B 只替换同一 `AssetId` 对应的图片、贴图、立绘、VFX 或结局卡文件；不得修改剧情节点 ID、触发顺序、状态字段和引擎接口。占位资源与正式资源必须保持相同资源 ID、尺寸基准、锚点和图层用途，替换后重新执行构建与测试。

### 14.4 本轮：翻页、占位人物、干预窗口的第三个选项

四条来自试玩反馈，逐条记下"改了什么 / 为什么这么改 / 怎么防止它再犯"。

**a) D2 08:00「成绩单来了」之后的画面没有人 —— 这是个真 bug（已修）**

那一刻的干预窗口画面（同学聚拢围观）是 `who:'common'` 的图，却在剧情里被写成按性别的
`S('crowd')` → 引擎把它拼成 `art.story.boy.crowd`，一个**根本不存在**的 AssetId →
`resolveArt()` 返回空数组 → 插画层被清空，画面上只剩一间空教室（以及当时还没删的占位小人）。
它活了很久而没人发现，因为**"少一张图"和"这一刻本来就没图"在运行时长得完全一样**：
不抛错、不 warn、不裂图，验收脚本也不会红（它查的是"这一帧有没有解码失败"，而不是"这一帧该不该有人"）。

三层修法，一层比一层靠后：

| 层 | 做法 | 会在什么时候响 |
|---|---|---|
| 引用入口 | `S()` 只收按性别的图、`C()` 只收不分性别的图，用错**加载期抛错** | `npm test` / `npm run build` 立刻红 |
| 内容对账 | `tests/unit/storyArt.test.ts`：每个时刻的每个槽位 × 男女两条路线，都必须解析到**已交付**的图 | 单测 |
| 真浏览器 | `tests/browser/smoke.mjs`：**17 个干预窗口逐个取样**"这一屏有立绘或插画" | 跑真机时 |

顺带修掉了它的"放大器"：`showArt()` 之后必须再 `showCast()` 一次。
插画在场 → 立绘让位；插画不在（没编排、或编排了但解析不出来）→ **立绘顶上**。
少了这一步，画面会从"场景底图 + 立绘"直接掉成"一张空场景"，也就是玩家看到的"没有人图"。

**b) 删掉通用占位小人（`components/student.ts`）**

它不分性别、跟正式立绘毫无关系，而且**越缺图越会出现**（真图在场时它才让位）。
删掉的范围是"一整组"：组件文件、`.student-layer` 容器、样式表里的 `.student` / `.student-svg` /
`[data-pose=…]`、地图系统与场景系统里的引用、`@keyframes float`。
`tests/unit/css.test.ts` 现在会断言**样式表里不许再出现 `.student`**（注释里可以留记录，规则不许留），
真浏览器验收也会数一遍 `.student, .student-layer, .student-svg` 必须是 0。

配套的一条数据层改动：**人物类资源缺图时不再画"占位方块 + 中文名"**（`content/assets.ts` 里
`character: true` 的那些，`src` 是空串），只有环境类（地点背景 / 叠加层 / 结局背景）保留占位色块 ——
缺底图是"环境没交"，标出来方便对账；而占位人物会**立在画面正中当主角**，比"这一刻没有人物图"糟得多。
`AssetLoader.preload()` 也顺带跳过空 src：空 src 在浏览器里会解析成"当前页面地址"，
每个缺图都要等一次真实的请求失败。

**c) 摸摸头换了落点**：策划案 §3.4 的彩蛋原来是点那个小人的，小人没了 → 改成**点立绘**。
因为立绘同时还是"继续下一句"的点击区，所以一次点击两件事都做（先摸头再翻页）——
这样绝不会出现"点了没反应"。每一幕限一次（`petUsed` 随 `scene.show()` 重置）。
这是"功能和手感"之间的取舍，已写进《待确认问题》6.10 请策划确认。

**d) 干预窗口的第三个按钮「这次，就让它过去」**（≤ `choiceSystem.runInteractions`）

- 永远可点、不消耗守护之光、**点了立刻继续**，不必等 10 秒（或 D6 的 15 秒）走完。
- 结算与"没来得及"**完全一致**：`missCount++`、`lastInterventionSuccess = false`、
  走 `onTimeout` 的台词与数值。两者在叙事上是同一件事：没有伸手。
- `runInteractions` 的返回值从 `string | null` 改成 `InterventionPick`
  （`{kind:'intervention',id} | {kind:'skip'} | {kind:'timeout'}`）——
  从前"超时"和"没选"只能共用一个 `null`，现在日志、文案与调试都能看出区别。
- 空格 / 回车在窗口里也等于它（`Engine.pressAdvance`），但有 **0.7 秒冷静期**：
  窗口是在玩家按掉最后一句开场白之后立刻打开的，那一按往往只是习惯性翻页。
  **冷静期只挡键盘、不挡点击** —— 这一点是被 `tests/smoke/advance.test.ts` 逼出来的
  （第一版把点击也挡了，于是"点了立刻继续"这条需求直接失效）。

**体积不变**（这轮没有新增图片）：`dist/index.html` 仍是 6.8 MB。

人物移动控制由美工 A 后期提供行走贴图和入口/出口素材，但**当前阶段不实现移动和碰撞**。当前地点按钮只作为开发调试入口，不能作为正式玩法；后期加入人物移动时，不改变 D1 → D7 剧情状态机，也不增加碰撞系统。

每个时刻必须有唯一 `momentId`、`onceKey`、天数、时间、地点、表现级别和下一目标；事件必须有窗口、成功、miss 与数值；台词必须标记 speaker，且不得新增 V1.2 之外文本。

---

## 15. 最终原则

```text
只实现 V1.2 定义的七日流程
不使用以前的策划案
不扩写剧情
不引入游戏引擎
内容写入 content/
机制写入 src/
节点可点击、可键盘操作、可保存、可恢复、可测试
最终交付单个可运行 HTML
```
